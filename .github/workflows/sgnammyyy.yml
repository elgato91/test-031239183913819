name: Test WARP Bridge Networking

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Pull immagine
        run: docker pull ghcr.io/kingcc/warproxy:latest

      - name: Crea rete bridge dedicata
        run: docker network create warp-test-net

      - name: Avvia 6 nodi su bridge dedicato (porte pubblicate singolarmente)
        run: |
          for i in 1 2 3 4 5 6; do
            PORT=$((40000 + i))
            docker run --name wp-$i \
              --network warp-test-net \
              -p $PORT:$PORT \
              -e SOCKS5_PORT=$PORT \
              -e DNS=2606:4700:4700::1111,1.1.1.1 \
              -d ghcr.io/kingcc/warproxy:latest

            DELAY=$(awk -v min=4 -v max=6 'BEGIN{srand('"$i"'+systime()); print min+rand()*(max-min)}')
            echo "Nodo $i avviato su porta $PORT (bridge), attendo ${DELAY}s..."
            sleep "$DELAY"
          done

          echo ""
          echo "=== Attesa registrazione (40s) ==="
          sleep 40

      - name: IP di uscita per nodo
        run: |
          for i in 1 2 3 4 5 6; do
            PORT=$((40000 + i))
            TRACE=$(curl -s --max-time 10 -x socks5h://127.0.0.1:$PORT https://www.cloudflare.com/cdn-cgi/trace)
            IP=$(echo "$TRACE" | grep "^ip=" | cut -d= -f2)
            WARP=$(echo "$TRACE" | grep "^warp=" | cut -d= -f2)
            echo "Nodo $i (porta $PORT): IP=$IP warp=$WARP"
          done

      - name: Pulizia
        if: always()
        run: |
          for i in 1 2 3 4 5 6; do
            docker rm -f wp-$i || true
          done
          docker network rm warp-test-net || true
