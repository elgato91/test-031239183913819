name: Test RAG Challenge 2026

on: 
  workflow_dispatch:

jobs:
  machine2_cdp_insocket:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Sincronizza Orologio di Sistema (NTP)
        run: |
          sudo systemctl stop systemd-timesyncd.service || true
          sudo sntp -sS pool.ntp.org || sudo ntpdate -s pool.ntp.org || true
          sudo systemctl start systemd-timesyncd.service || true

      - name: Installazione Font di Sistema (Requisito per Hash Canvas)
        run: |
          sudo apt-get update
          echo "ttf-mscorefonts-installer msttcorefonts/accepted-mscorefonts-eula select true" | sudo debconf-set-selections
          sudo apt-get install -y ttf-mscorefonts-installer \
            fonts-noto-color-emoji fonts-freefont-ttf fonts-unifont \
            fonts-ipafont-gothic fonts-wqy-zenhei fonts-tlwg-loma-otf || true
          fc-cache -f -v

      - name: Installazione Dipendenze Scraper
        run: |
          pip install "camoufox[geoip]" beautifulsoup4 requests "requests[socks]"
          # Installazione delle librerie di sistema richieste da Firefox per funzionare senza problemi
          playwright install-deps firefox

      - name: Setup 3 WARProxy Docker Containers (IPv6)
        run: |
          docker pull ghcr.io/kingcc/warproxy:latest

          for i in 1 2 3; do
            PORT=$((40000 + i))
            echo "Avvio container warproxy-$i su porta $PORT..."
            docker run --name warproxy-$i \
              --network host \
              -e TZ=Europe/Rome \
              -e SOCKS5_PORT=$PORT \
              -e DNS=2606:4700:4700::1111,2606:4700:4700::1001 \
              -e VERBOSE=true \
              -d ghcr.io/kingcc/warproxy:latest
            
            # Scagliona gli avvii per non incorrere nel rate-limit di Cloudflare WARP
            sleep 15
          done

          echo "Container attivi:"
          docker ps -a

      - name: Verifica IP IPv6 di uscita sui 3 Proxy
        run: |
          for i in 1 2 3; do
            PORT=$((40000 + i))
            echo "=================================================="
            echo "VERIFICA IP CONTAINER $i ($PORT):"
            for attempt in 1 2 3; do
              curl -s --connect-timeout 8 --proxy socks5h://127.0.0.1:$PORT https://api6.ipify.org?format=json && break || {
                echo "Tentativo $attempt fallito per porta $PORT, attesa..."
                sleep 5
              }
            done
          done
          echo "=================================================="

      - name: Download Binario Camoufox (Firefox Custom)
        run: |
          python -m camoufox fetch

      - name: Creazione script Python (Macchina 2)
        run: |
          cat << 'EOF' > m2_insocket.py
          import time
          import sys
          import random
          import requests
          from camoufox.sync_api import Camoufox
          from bs4 import BeautifulSoup
          from urllib.parse import urlparse, parse_qs

          TARGETS = [
              "https://api6.ipify.org?format=json", 
              "https://tls.browserleaks.com/json",
              "https://bot.sannysoft.com/",
              "https://abrahamjuliot.github.io/creepjs/",
              "https://pixelscan.net/",
              "https://bot.incolumitas.com/",
              "https://www.deviceinfo.me/",
              "https://www.google.com/search?q=cosa+vedere+roma&hl=it&lr=lang_it", 
              "https://www.bing.com/search?q=cosa+vedere+roma",
              "https://www.eater.com/rome",
              "https://www.bloomberg.com/markets/stocks",
              "https://www.morningstar.com/stocks/xnas/aapl/quote",
              "https://finance.yahoo.com/quote/AAPL/key-statistics",
              "https://www.yelp.com/search?find_desc=Restaurants&find_loc=San+Francisco%2C+CA",
              "https://www.passportindex.org/passport/italy/",
              "https://guide.michelin.com/en/it/lazio/roma/restaurants",
              "https://www.cntraveler.com/gallery/best-restaurants-in-rome",
              "https://www.facebook.com/groups/1373361046396315/posts/2860154274383644/",
              "https://katieparla.com/budget-dining-in-rome-revised/",
              "https://www.google.com/search?q=migliore+carbonara+roma&gbv=1&hl=it&lr=lang_it", 
              "https://html.duckduckgo.com/html/?q=migliore+carbonara+roma",
              "https://www.bing.com/search?q=migliore+carbonara+roma",
              "https://www.startpage.com/sp/search?query=migliore+carbonara+roma",
              "https://search.yahoo.com/search?p=migliore+carbonara+roma",
              "https://www.reddit.com/r/rome/comments/1cn61n5/must_try_restaurants_in_rome/",
              "https://newromantimes.substack.com/p/a-very-subjective-list-of-the-best",
              "https://www.canadianinsider.com/money-is-moving"
          ]

          DIAGNOSTIC_SITES = ["ipify", "browserleaks", "sannysoft", "creepjs", "pixelscan", "incolumitas", "deviceinfo"]

          if len(sys.argv) < 2:
              sys.exit(1)

          node_id = int(sys.argv[1])
          ACTIVE_PORT = 40000 + node_id
          MY_TARGETS = TARGETS[(node_id-1)*9 : node_id*9]

          def extract_text(html_content, url):
              soup = BeautifulSoup(html_content, 'html.parser')
              text = soup.get_text(separator=' ', strip=True)
              if any(site in url for site in DIAGNOSTIC_SITES):
                  return text
              return text[:5000]

          def get_query_from_url(url, default="migliore carbonara roma"):
              try:
                  parsed = urlparse(url)
                  queries = parse_qs(parsed.query)
                  for param in ['q', 'query', 'p']:
                      if param in queries:
                          return queries[param][0]
              except Exception:
                  pass
              return default

          # Verifica semplicemente che il container proxy locale risponda
          def wait_for_proxy(port, timeout=45):
              print(f"[NODO {node_id}] Verifica connettività proxy locale su porta {port}...", flush=True)
              start_time = time.time()
              proxies = {"http": f"socks5h://127.0.0.1:{port}", "https": f"socks5h://127.0.0.1:{port}"}
              while time.time() - start_time < timeout:
                  try:
                      r = requests.get("https://api6.ipify.org?format=json", proxies=proxies, timeout=5)
                      if r.status_code == 200:
                          print(f"[NODO {node_id}] SOCKS5 Proxy pronto: {r.json().get('ip')}", flush=True)
                          return True
                  except Exception:
                      time.sleep(2)
              return False

          if not wait_for_proxy(ACTIVE_PORT):
              print(f"[NODO {node_id}] Proxy o IPv6 non pronto. Termino.", flush=True)
              sys.exit(1)

          profile_dir = f"./profile_node_{node_id}"

          # Avvio del browser stealth Firefox Camoufox in headless nativo
          with Camoufox(
              headless=True,                             # Nessun bisogno di Xvfb! 100% invisibile.
              proxy={"server": f"socks5://127.0.0.1:{ACTIVE_PORT}"},
              geoip=True,                                # Allinea fuso orario, coordinate e lingua in base all'IP
              fingerprint_preset=True,                   # Fingerprint reali salvati per aggirare Morningstar e CF
              humanize=True,                             # Movimenti del cursore e digitazione simulati
              persistent_context=True,                   # Mantiene sessione e cookie tra i caricamenti
              user_data_dir=profile_dir,
              block_images=True,                         # Disabilita le immagini per risparmiare traffico e velocizzare i nodi
          ) as browser:

              results = f"\n{'#'*60}\nREPORT NODO {node_id} (PORTA {ACTIVE_PORT})\n{'#'*60}\n"

              for url in MY_TARGETS:
                  results += f"\n{'='*50}\nTARGET: {url}\n{'='*50}\n"
                  print(f"[NODO {node_id}] -> {url}", flush=True)
                  try:
                      page = browser.new_page()
                      query_to_search = get_query_from_url(url)

                      if "yelp.com" in url or "yelp.it" in url:
                          page.goto("https://www.yelp.com/", wait_until="domcontentloaded", timeout=30000)
                          time.sleep(3)
                          page.goto(url, wait_until="domcontentloaded", timeout=30000)
                      elif "startpage.com" in url:
                          page.goto("https://www.startpage.com/", wait_until="domcontentloaded", timeout=30000)
                          try:
                              page.locator("input#q").type(query_to_search, delay=random.randint(50, 150), timeout=3000)
                              page.keyboard.press("Enter")
                              page.wait_for_load_state("domcontentloaded", timeout=30000)
                          except Exception: pass
                      elif "html.duckduckgo.com" in url:
                          page.goto("https://html.duckduckgo.com/", wait_until="domcontentloaded", timeout=30000)
                          try:
                              page.locator("input[name=q]").type(query_to_search, delay=random.randint(50, 150), timeout=3000)
                              page.keyboard.press("Enter")
                              page.wait_for_load_state("domcontentloaded", timeout=30000)
                          except Exception: pass
                      else:
                          page.goto(url, wait_until="domcontentloaded", timeout=30000)

                      # Trigger manuale Pixelscan
                      if "pixelscan.net" in url:
                          try:
                              btn = page.locator("text=Scan My Browser Now")
                              if btn.is_visible(timeout=3000):
                                  btn.click()
                          except Exception: pass

                      # Risoluzione Cloudflare Turnstile
                      try:
                          cf_iframe = page.frame_locator("iframe[src*='challenges.cloudflare.com']")
                          checkbox = cf_iframe.locator("input[type='checkbox']")
                          if checkbox.is_visible(timeout=3000):
                              checkbox.click()
                              time.sleep(4)
                      except Exception: pass

                      # Cookie consent bypass
                      if not any(x in url for x in ["facebook.com", "reddit.com", "yelp"]):
                          try:
                              consent_selectors = [
                                  "button:has-text('Accetta tutto')", "button:has-text('Accept all')",
                                  "button:has-text('Consenti')", "button:has-text('I agree')"
                              ]
                              for selector in consent_selectors:
                                  btn = page.locator(selector)
                                  if btn.is_visible(timeout=1000):
                                      btn.click()
                                      break
                          except Exception: pass

                      # Scroll simulato
                      try:
                          for _ in range(3):
                              page.mouse.wheel(0, random.randint(300, 600))
                              time.sleep(random.uniform(0.8, 1.5))
                      except Exception: pass

                      # Attese mirate post-caricamento
                      if "creepjs" in url: time.sleep(15) 
                      elif "pixelscan" in url: time.sleep(8)
                      elif any(t in url for t in ["sannysoft", "browserleaks", "incolumitas", "deviceinfo", "finance.yahoo"]): time.sleep(5)
                      else: time.sleep(3)

                      raw_html = page.content()
                      results += f"TESTO ESTRATTO:\n{extract_text(raw_html, url)}\n"
                      page.close()

                  except Exception as e:
                      print(f"[NODO {node_id}] Errore su {url}: {e}", flush=True)
                      results += f"ERRORE GLOBALE: {str(e)}\n"
                      try: page.close()
                      except Exception: pass

          with open(f"output_node_{node_id}.txt", "w", encoding="utf-8") as f:
              f.write(results)
          EOF

      - name: Esecuzione Scraping Parallelo (3 Nodi indipendenti)
        run: |
          echo "Avvio processi paralleli in background..."
          python -u m2_insocket.py 1 > node1.log 2>&1 &
          python -u m2_insocket.py 2 > node2.log 2>&1 &
          python -u m2_insocket.py 3 > node3.log 2>&1 &
          
          # Attende il completamento di tutti i processi in background
          wait

          echo "=== LOG NODO 1 ==="
          cat node1.log || true
          echo "=== LOG NODO 2 ==="
          cat node2.log || true
          echo "=== LOG NODO 3 ==="
          cat node3.log || true
          echo "=================="

          # Crea i file vuoti nel caso un nodo fosse andato in crash irreversibile
          touch output_node_1.txt output_node_2.txt output_node_3.txt
          
          # Assembla il report finale
          cat output_node_1.txt output_node_2.txt output_node_3.txt > output_macchina2_cdp.txt

      - name: Upload Artifact Macchina 2
        uses: actions/upload-artifact@v4
        with:
          name: Risultati_Macchina2
          path: output_macchina2_cdp.txt
