name: AI Agentic Scraper PoC (SeleniumBase UC + Gemini 3 Live)

on: 
  workflow_dispatch:

jobs:
  agentic_scraping:
    runs-on: ubuntu-latest
    
    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

    steps:
      - name: Setup WARProxy (Anti-Ban)
        run: |
          docker pull ghcr.io/kingcc/warproxy:latest
          docker run --name warproxy-1 --network host \
            -e SOCKS5_PORT=40001 -e DNS=2606:4700:4700::1111,2606:4700:4700::1001 \
            -d ghcr.io/kingcc/warproxy:latest
          sleep 5

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Installazione Dipendenze (SeleniumBase, SOCKS e GenAI SDK)
        # Installiamo seleniumbase e selenium-wire per abilitare l'intercettazione dei pacchetti in UC Mode
        run: |
          pip install seleniumbase selenium-wire google-genai "requests[socks]" PySocks
          seleniumbase install chromedriver

      - name: Avvio Xvfb (Display virtuale per UC Mode Headful)
        # Indispensabile per far girare SeleniumBase in modalità headed (non rilevabile) su Linux
        run: |
          Xvfb :99 -screen 0 1920x1080x24 &
          echo "DISPLAY=:99" >> $GITHUB_ENV

      - name: Fase 1 - L'Ariete (Intercettazione Rete con SeleniumBase UC Mode)
        run: |
          cat << 'EOF' > interceptor.py
          import json
          import time
          from seleniumbase import SB

          captured_requests = []

          # Inizializziamo SeleniumBase con:
          # uc=True -> Undetected ChromeDriver (Bypass dei blocchi)
          # wire=True -> Abilita selenium-wire per intercettare il traffico di rete
          # headless=False -> Headed reale su schermo virtuale Xvfb
          with SB(uc=True, wire=True, headless=False, proxy="socks5://127.0.0.1:40001") as sb:
              url = "https://www.google.com/maps/place/Coro/@42.7179111,12.1116244,17z/data=!4m8!3m7!1s0x132ecd778417bda5:0x4e6e6043f0ea67e1!8m2!3d42.7179111!4d12.1116244!9m1!1b1!16s%2Fg%2F11l23qq660"
              print("Apertura di Google Maps tramite SeleniumBase UC Mode...")
              sb.open(url)
              sb.sleep(5)
              
              # Gestione del consenso dei cookie se presente
              try:
                  if sb.is_element_visible('button:has-text("Accetta tutto")'):
                      sb.click('button:has-text("Accetta tutto")')
                      print("Consenso cookie cliccato.")
                      sb.sleep(4)
              except Exception as e:
                  print(f"Nessun popup di consenso rilevato: {e}")

              # Scorrimento per innescare le chiamate di rete AJAX
              print("Scorrimento della pagina per innescare le API...")
              try:
                  for _ in range(5):
                      # Tasto Page Down per scorrere l'interfaccia
                      sb.press_keys('body', '\ue00f')
                      sb.sleep(2)
              except Exception as e:
                  print(f"Errore durante lo scorrimento: {e}")

              # Salvataggio screenshot di debug dello schermo virtuale
              sb.save_screenshot("page_state.png")
              print("Screenshot dello stato della pagina salvato come page_state.png")

              # Analisi dei pacchetti intercettati da selenium-wire
              print("Analisi del traffico di rete catturato...")
              for request in sb.driver.requests:
                  url_str = request.url
                  if "listugc" in url_str or "preview/review" in url_str:
                      captured_requests.append({
                          "url": url_str,
                          "method": request.method,
                          "headers": dict(request.headers),
                          "post_data": request.body.decode('utf-8', errors='ignore') if request.body else None
                      })

          if captured_requests:
              # Isoliamo la richiesta più grande che contiene il payload
              best_request = max(captured_requests, key=lambda r: len(r['url']))
              with open("intercepted.json", "w") as f:
                  json.dump(best_request, f)
              print(f"Richiesta intercettata con successo! URL: {best_request['url'][:80]}...")
          else:
              print("ERRORE: NESSUNA RICHIESTA 'listugc' INTERCETTATA.")
              # Creiamo un file vuoto per evitare il crash immediato del workflow
              with open("intercepted.json", "w") as f:
                  json.dump({}, f)
          EOF
          python interceptor.py

      - name: Fase 2 - Il Cervello (AI Agent via Gemini 3 Live API)
        run: |
          cat << 'EOF' > ai_agent.py
          import asyncio
          import os
          import sys
          import json
          from google import genai
          from google.genai import types

          async def main():
              api_key = os.environ.get("GEMINI_API_KEY")
              if not api_key:
                  print("ERRORE CRITICO: API Key non trovata. Aggiungi GEMINI_API_KEY nei secrets.")
                  sys.exit(1)

              client = genai.Client(api_key=api_key)
              
              config = types.LiveConnectConfig(
                  response_modalities=["AUDIO"],
                  output_audio_transcription=types.AudioTranscriptionConfig()
              )
              model = "gemini-3.1-flash-live-preview"

              try:
                  with open("intercepted.json", "r") as f:
                      content = json.load(f)
                      if not content:
                          print("Il file delle intercettazioni è vuoto. Impossibile procedere con l'AI.")
                          sys.exit(1)
                  req_data = json.dumps(content, indent=2)
              except Exception as e:
                  print(f"Errore di lettura del file intercepted.json: {e}")
                  sys.exit(1)

              prompt = f"""
              Sei un programmatore Python esperto di web scraping e reverse engineering.
              Il mio browser ha intercettato questa richiesta HTTP interna verso le API di Google Maps:
              
              {req_data}
              
              Il tuo compito è scrivere uno script Python usando la libreria 'requests' che replica questa chiamata HTTP (usando lo stesso URL e headers intercettati).
              Lo script deve:
              1. Importare la libreria 'requests'.
              2. Impostare le variabili url e headers (copiando i dati intercettati).
              3. Fare la richiesta HTTP (GET o POST in base al method). Usa proxies={{"http": "socks5://127.0.0.1:40001", "https": "socks5://127.0.0.1:40001"}} per il proxy.
              4. Salvare il testo crudo della risposta (response.text) in un file chiamato 'raw_data.txt'.
              
              RESTITUISCI SOLO IL CODICE PYTHON PURO VALIDO. Non usare blocchi markdown (come ```python), nessuna spiegazione iniziale o finale. Solo codice.
              """

              print("Connessione in corso a Gemini 3.1 Live API tramite WebSocket...")
              generated_code = []

              async with client.aio.live.connect(model=model, config=config) as session:
                  print("Connessione asincrona stabilita.")
                  
                  await session.send_realtime_input(text=prompt)
                  print("Prompt inviato. Ricezione della trascrizione del codice...")

                  async_for = session.receive()
                  async for response in async_for:
                      server_content = response.server_content
                      if server_content:
                          if server_content.output_transcription and server_content.output_transcription.text:
                              text_chunk = server_content.output_transcription.text
                              print(text_chunk, end="", flush=True)
                              generated_code.append(text_chunk)
                          
                          if server_content.turn_complete:
                              break

              code = "".join(generated_code).strip()
              
              if code.startswith("```python"): code = code[9:]
              if code.startswith("```"): code = code[3:]
              if code.endswith("```"): code = code[:-3]
              code = code.strip()

              with open("fast_scraper.py", "w") as f:
                  f.write(code)
              print("\n[AI Agent] Codice Python salvato in fast_scraper.py!")

          if __name__ == "__main__":
              asyncio.run(main())
          EOF
          python ai_agent.py

      - name: Fase 3 - Velocità della Luce (Esecuzione Codice AI)
        run: |
          if [ -f fast_scraper.py ]; then
            echo "==================================="
            echo "Eseguo lo script scritto da Gemini 3.1 Live..."
            cat fast_scraper.py
            echo "==================================="
            python fast_scraper.py
            echo "Esecuzione completata!"
          else
            echo "Nessuno script generato dall'AI da eseguire."
          fi

      - name: Upload Artefatti Finali
        uses: actions/upload-artifact@v4
        with:
          name: AI_Reverse_Engineering_PoC
          path: |
            intercepted.json
            fast_scraper.py
            raw_data.txt
            page_state.png
