name: Test RAG Challenge 2026

on:
  workflow_dispatch:

jobs:
  machine2_cdp_insocket:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Installazione Font di Sistema (Inclusi Font Microsoft)
        run: |
          sudo apt-get update
          # Accettazione automatica non interattiva dell'EULA Microsoft per i font core di Windows
          echo "ttf-mscorefonts-installer msttcorefonts/accepted-mscorefonts-eula select true" | sudo debconf-set-selections
          sudo apt-get install -y ttf-mscorefonts-installer \
            fonts-noto-color-emoji fonts-freefont-ttf fonts-unifont \
            fonts-ipafont-gothic fonts-wqy-zenhei fonts-tlwg-loma-otf || true
          fc-cache -f -v

      - name: Installazione Dipendenze Scraper
        run: |
          # Installiamo requests[socks] per supportare il controllo di connettività dei proxy SOCKS5h in Python
          pip install "cloakbrowser[geoip]" beautifulsoup4 playwright requests "requests[socks]"
          playwright install-deps chromium

      - name: Setup 3 WARProxy Docker Containers (Active & Parallel IPv6-only)
        run: |
          # Pull dell'immagine ufficiale ottimizzata
          docker pull ghcr.io/kingcc/warproxy:latest

          # CONTAINER 1 - Porta locale 40001 (Forzato IPv6 DNS)
          docker run --name warproxy-1 \
            --network host \
            -e TZ=Europe/Rome \
            -e SOCKS5_PORT=40001 \
            -e DNS=2606:4700:4700::1111,2606:4700:4700::1001 \
            -e VERBOSE=true \
            -d ghcr.io/kingcc/warproxy:latest

          # CONTAINER 2 - Porta locale 40002 (Forzato IPv6 DNS)
          docker run --name warproxy-2 \
            --network host \
            -e TZ=Europe/Rome \
            -e SOCKS5_PORT=40002 \
            -e DNS=2606:4700:4700::1111,2606:4700:4700::1001 \
            -e VERBOSE=true \
            -d ghcr.io/kingcc/warproxy:latest

          # CONTAINER 3 - Porta locale 40003 (Forzato IPv6 DNS)
          docker run --name warproxy-3 \
            --network host \
            -e TZ=Europe/Rome \
            -e SOCKS5_PORT=40003 \
            -e DNS=2606:4700:4700::1111,2606:4700:4700::1001 \
            -e VERBOSE=true \
            -d ghcr.io/kingcc/warproxy:latest

          # Attesa stabilizzazione nativa dei proxy (WireGuard handshake)
          sleep 15
          docker ps -a

      - name: Verifica IP IPv6 di uscita sui 3 Proxy
        run: |
          echo "=================================================="
          echo "VERIFICA IP CONTAINER 1 (40001):"
          curl --proxy socks5h://127.0.0.1:40001 https://api64.ipify.org?format=json || echo "Proxy 1 fallito!"
          
          echo "=================================================="
          echo "VERIFICA IP CONTAINER 2 (40002):"
          curl --proxy socks5h://127.0.0.1:40002 https://api64.ipify.org?format=json || echo "Proxy 2 fallito!"

          echo "=================================================="
          echo "VERIFICA IP CONTAINER 3 (40003):"
          curl --proxy socks5h://127.0.0.1:40003 https://api64.ipify.org?format=json || echo "Proxy 3 fallito!"
          echo "=================================================="

      - name: Avvio Xvfb (Virtual Display Stabile)
        run: |
          Xvfb :99 -screen 0 1920x1080x24 &
          echo "DISPLAY=:99" >> $GITHUB_ENV

      - name: Creazione script Python (Macchina 2)
        run: |
          cat << 'EOF' > m2_insocket.py
          import time
          import sys
          import random
          import requests
          from cloakbrowser import launch_context
          from bs4 import BeautifulSoup
          from urllib.parse import urlparse, parse_qs

          TARGETS = [
              "https://api64.ipify.org?format=json",
              "https://tls.browserleaks.com/json",
              "https://bot.sannysoft.com/",
              "https://abrahamjuliot.github.io/creepjs/",
              "https://pixelscan.net/",
              "https://bot.incolumitas.com/",
              "https://www.deviceinfo.me/",
              "https://www.google.com/search?q=cosa+vedere+roma",
              "https://www.bing.com/search?q=cosa+vedere+roma",
              "https://www.eater.com/rome",
              "https://www.bloomberg.com/markets/stocks",
              "https://www.morningstar.com/stocks/xnas/aapl/quote",
              "https://finance.yahoo.com/quote/AAPL/key-statistics",
              "https://www.yelp.com/search?find_desc=Restaurants&find_loc=San+Francisco%2C+CA",
              "https://www.passportindex.org/passport/italy/",
              "https://guide.michelin.com/en/it/lazio/roma/restaurants",
              "https://www.cntraveler.com/gallery/best-restaurants-in-rome",
              "https://www.facebook.com/groups/1373361046396315/posts/2860154274383644/",
              "https://katieparla.com/budget-dining-in-rome-revised/",
              "https://www.google.com/search?q=migliore+carbonara+roma&gbv=1",
              "https://html.duckduckgo.com/html/?q=migliore+carbonara+roma",
              "https://www.bing.com/search?q=migliore+carbonara+roma",
              "https://www.startpage.com/sp/search?query=migliore+carbonara+roma",
              "https://search.yahoo.com/search?p=migliore+carbonara+roma",
              "https://www.reddit.com/r/rome/comments/1cn61n5/must_try_restaurants_in_rome/",
              "https://newromantimes.substack.com/p/a-very-subjective-list-of-the-best",
              "https://www.canadianinsider.com/money-is-moving"
          ]

          DIAGNOSTIC_SITES = ["ipify", "browserleaks", "sannysoft", "creepjs", "pixelscan", "incolumitas", "deviceinfo"]

          # 1. Parsing dell'argomento node_id per dividere i target a un terzo ciascuno
          if len(sys.argv) < 2:
              print("Errore: specifica il node_id (1, 2 o 3)", flush=True)
              sys.exit(1)

          node_id = int(sys.argv[1])
          if node_id == 1:
              ACTIVE_PORT = 40001
              MY_TARGETS = TARGETS[0:9]
          elif node_id == 2:
              ACTIVE_PORT = 40002
              MY_TARGETS = TARGETS[9:18]
          elif node_id == 3:
              ACTIVE_PORT = 40003
              MY_TARGETS = TARGETS[18:27]
          else:
              print("Errore: node_id deve essere 1, 2 o 3", flush=True)
              sys.exit(1)

          print(f"=== Nodo {node_id} avviato su porta {ACTIVE_PORT} ===")
          print(f"Target assegnati (Totale: {len(MY_TARGETS)}):", flush=True)
          for t in MY_TARGETS:
              print(f" - {t}", flush=True)

          def extract_text(html_content, url):
              soup = BeautifulSoup(html_content, 'html.parser')
              text = soup.get_text(separator=' ', strip=True)
              if any(site in url for site in DIAGNOSTIC_SITES):
                  return text
              return text[:5000]

          def get_query_from_url(url, default="migliore carbonara roma"):
              try:
                  parsed = urlparse(url)
                  queries = parse_qs(parsed.query)
                  for param in ['q', 'query', 'p']:
                      if param in queries:
                          return queries[param][0]
              except Exception:
                  pass
              return default

          def handle_cookie_consent(page):
              try:
                  consent_selectors = [
                      "button:has-text('Accetta tutto')",
                      "button:has-text('Accept all')",
                      "button:has-text('Consenti')",
                      "button:has-text('I agree')",
                      "button:has-text('Accetto')",
                      "button:has-text('Agree')",
                      "button:has-text('Accept')",
                      "button:has-text('OK')",
                      "button:has-text('Allow all')",
                      "button:has-text('Consenti l\\'uso dei cookie')",
                      "#L2AGLb"
                  ]
                  for selector in consent_selectors:
                      btn = page.locator(selector)
                      if btn.is_visible():
                          btn.click(timeout=1500)
                          time.sleep(0.5)
                          print(f"Cookie bypassato: {selector}", flush=True)
                          break
              except Exception:
                  pass

          # Polling attivo per attendere che il rispettivo container sia pronto a instradare l'IPv6
          def wait_for_proxy(port, timeout=45):
              print(f"In attesa del proxy sulla porta {port}...", flush=True)
              start_time = time.time()
              proxies = {"http": f"socks5h://127.0.0.1:{port}", "https": f"socks5h://127.0.0.1:{port}"}
              while time.time() - start_time < timeout:
                  try:
                      r = requests.get("https://api64.ipify.org?format=json", proxies=proxies, timeout=3)
                      if r.status_code == 200:
                          ip = r.json().get("ip")
                          if ip and ":" in ip: # Valida l'uscita in IPv6
                              print(f"Tunnel pronto! IPv6 di WARP rilevato: {ip}", flush=True)
                              return True
                  except Exception:
                      pass
                  time.sleep(2)
              return False

          if not wait_for_proxy(ACTIVE_PORT):
              print(f"Errore: il proxy sulla porta {ACTIVE_PORT} non risponde in IPv6.", flush=True)
              sys.exit(1)

          random_seed = str(random.randint(100000, 999999))
          print(f"Avviando CloakBrowser per il nodo {node_id}...", flush=True)

          # GeoIP nativo abilitato su SOCKS5h: CloakBrowser imposterà autonomamente la corretta timezone dell'IP di uscita
          context = launch_context(
              headless=False,
              humanize=True,
              proxy=f"socks5://127.0.0.1:{ACTIVE_PORT}",
              geoip=True,
              viewport={"width": 1920, "height": 950},
              args=[
                  "--no-sandbox", 
                  "--disable-dev-shm-usage",
                  f"--fingerprint={random_seed}",
                  "--fingerprint-storage-quota=5000",   
                  "--fingerprint-windows-font-metrics"  
              ]
          )

          results = f"\n=== RISULTATI SCRAPING NODO {node_id} (PORTA {ACTIVE_PORT}) ===\n"

          for url in MY_TARGETS:
              results += f"\n{'='*50}\nTARGET: {url}\n{'='*50}\n"
              print(f"[Nodo {node_id}] Analizzando: {url}", flush=True)
              try:
                  page = context.new_page()
                  
                  # INIEZIONE ATTIVA DI POLYFILL
                  page.add_init_script("""
                      if (!('ContactsManager' in window)) {
                          window.ContactsManager = class ContactsManager {
                              async getProperties() { return ['name', 'email', 'tel', 'address', 'icon']; }
                              async select() { return []; }
                          };
                          Object.defineProperty(navigator, 'contacts', {
                              get: () => new window.ContactsManager(),
                              configurable: true,
                              enumerable: true
                          });
                      }

                      if (!('ContentIndex' in window)) {
                          window.ContentIndex = class ContentIndex {
                              async add() {}
                              async delete() {}
                              async getAll() { return []; }
                          };
                      }

                      if (window.NetworkInformation && !('downlinkMax' in window.NetworkInformation.prototype)) {
                          Object.defineProperty(window.NetworkInformation.prototype, 'downlinkMax', {
                              get: () => 10,
                              configurable: true,
                              enumerable: true
                          });
                      }
                      if (navigator.connection) {
                          if (!('downlinkMax' in navigator.connection)) {
                              Object.defineProperty(navigator.connection, 'downlinkMax', {
                                  get: () => 10,
                                  configurable: true,
                                  enumerable: true
                              });
                          }
                          Object.defineProperty(navigator.connection, 'rtt', {
                              get: () => 100, 
                              configurable: true,
                              enumerable: true
                          });
                      }
                  """)

                  query_to_search = get_query_from_url(url)

                  # GESTIONE SPECIFICA YELP
                  if "yelp.com" in url or "yelp.it" in url:
                      page.goto("https://www.yelp.com/", wait_until="domcontentloaded", timeout=25000)
                      time.sleep(3)
                      page.goto(url, wait_until="domcontentloaded", timeout=25000)
                  
                  # Navigazione simulata per Startpage
                  elif "startpage.com/sp/search" in url:
                      page.goto("https://www.startpage.com/", wait_until="domcontentloaded", timeout=25000)
                      time.sleep(2)
                      try:
                          search_input = page.locator("input#q")
                          search_input.type(query_to_search, delay=100, timeout=2000)
                          page.keyboard.press("Enter")
                      except Exception as e:
                          pass
                  
                  # Navigazione simulata per DuckDuckGo HTML
                  elif "html.duckduckgo.com/html" in url:
                      page.goto("https://html.duckduckgo.com/", wait_until="domcontentloaded", timeout=25000)
                      time.sleep(2)
                      try:
                          search_input = page.locator("input[name=q]")
                          search_input.type(query_to_search, delay=100, timeout=2000)
                          page.keyboard.press("Enter")
                      except Exception as e:
                          pass
                      
                  else:
                      page.goto(url, wait_until="domcontentloaded", timeout=25000)
                      if not any(x in url for x in ["facebook.com", "reddit.com", "yelp"]):
                          handle_cookie_consent(page)
                  
                  # GESTIONE SPECIFICA YAHOO FINANCE
                  if "finance.yahoo.com" in url:
                      try:
                          page.wait_for_selector('section[data-testid="qsp-statistics"]', timeout=5000)
                      except Exception:
                          pass
                      time.sleep(1)

                  # Scorrimento simulato
                  try:
                      for _ in range(3):
                          page.mouse.wheel(0, 300)
                          time.sleep(1)
                  except Exception:
                      pass

                  # Tempi di attesa bilanciati
                  if "creepjs" in url:
                      time.sleep(20) 
                  elif "pixelscan" in url:
                      time.sleep(10)
                  elif any(test in url for test in ["sannysoft", "browserleaks", "incolumitas", "deviceinfo"]):
                      time.sleep(5)
                  else:
                      time.sleep(4)

                  raw_html = page.content()
                  results += f"TESTO ESTRATTO:\n{extract_text(raw_html, url)}\n"
                  page.close()

              except Exception as e:
                  print(f"[Nodo {node_id}] Errore critico su {url}: {e}", flush=True)
                  results += f"ERRORE GLOBALE: {str(e)}\n"
                  try:
                      page.close()
                  except Exception:
                      pass

          context.close()

          # Salvataggio del report parziale di questo nodo
          with open(f"output_node_{node_id}.txt", "w", encoding="utf-8") as f:
              f.write(results)
          EOF

      - name: Esecuzione Scraping parallelo (3 Nodi indipendenti)
        run: |
          # Avvio dei 3 processi Python in background
          python -u m2_insocket.py 1 > node1.log 2>&1 &
          python -u m2_insocket.py 2 > node2.log 2>&1 &
          python -u m2_insocket.py 3 > node3.log 2>&1 &
          
          # Attende che tutti e tre i processi paralleli abbiano concluso
          wait

          # Stampa i log dei nodi per ispezione
          echo "=== LOG NODO 1 ==="
          cat node1.log || true
          echo "=== LOG NODO 2 ==="
          cat node2.log || true
          echo "=== LOG NODO 3 ==="
          cat node3.log || true
          echo "=================="

          # Concatenazione dei file di output in un unico report finale
          cat output_node_1.txt output_node_2.txt output_node_3.txt > output_macchina2_cdp.txt

      - name: Upload Artifact Macchina 2
        uses: actions/upload-artifact@v4
        with:
          name: Risultati_Macchina2
          path: output_macchina2_cdp.txt
