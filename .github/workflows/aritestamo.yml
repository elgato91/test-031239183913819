name: Test RAG Challenge 2026 Università di Roma Corso di Computer Science

on: 
  workflow_dispatch:

jobs:
  machine2_cdp_insocket:
    runs-on: ubuntu-latest
    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      TARGET_TOPIC: "Orvieto"
    steps:

      - name: Installazione Font di Sistema
        run: |
          sudo apt-get update
          echo "ttf-mscorefonts-installer msttcorefonts/accepted-mscorefonts-eula select true" | sudo debconf-set-selections
          sudo apt-get install -y ttf-mscorefonts-installer \
            fonts-noto-color-emoji fonts-freefont-ttf fonts-unifont \
            fonts-ipafont-gothic fonts-wqy-zenhei fonts-tlwg-loma-otf || true
          
          mkdir -p ~/.local/share/fonts/windows
          cp /usr/share/fonts/truetype/msttcorefonts/*.ttf ~/.local/share/fonts/windows/ || true
          cp /usr/share/fonts/truetype/msttcorefonts/*.TTF ~/.local/share/fonts/windows/ || true
          fc-cache -f -v

      - name: Installazione Dipendenze Scraper
        run: |
          pip install "cloakbrowser[geoip]" beautifulsoup4 playwright requests "requests[socks]" curl-cffi markdownify google-genai
          playwright install-deps chromium

      - name: Avvio Xvfb (Virtual Display Headful)
        run: |
          Xvfb :99 -screen 0 1920x1080x24 &
          echo "DISPLAY=:99" >> $GITHUB_ENV

      - name: Pre-inizializzazione Cache CloakBrowser
        run: |
          python -m cloakbrowser install

      - name: Setup Container WARP (IPv6)
        run: |
          docker pull ghcr.io/kingcc/warproxy:latest

          for i in {1..6}; do
            PORT=$((40000 + i))
            echo "Avvio container warproxy-$i sulla porta $PORT..."
            docker run --name warproxy-$i \
              --network host \
              -e SOCKS5_PORT=$PORT \
              -e DNS=2606:4700:4700::1111,2606:4700:4700::1001 \
              -e VERBOSE=true \
              -d ghcr.io/kingcc/warproxy:latest
            sleep 0.5
          done
          
          echo "Attesa stabilizzazione tunnel WARP..."
          sleep 6

      - name: Inizializzazione Code DISTRIBUITE - STEP 1
        run: |
          cat << 'EOF' > m2_step1_setup.py
          import json
          TARGETS = [
              "https://10cose.it/orvieto/cosa-vedere-orvieto",
              "https://4viteinvacanza.it/itinerari/orvieto-con-i-bambini",
              "https://accessibilitacentristorici.it/ztl/Umbria_en/orvieto.html",
              "https://accessibilitacentristorici.it/ztl/umbria/orvieto/parcheggio_orvieto.html",
              "https://accessibilitacentristorici.it/ztl/umbria/parcheggio_orvieto.html",
              "https://acooknotmad.com/2016/11/we-love-orvieto.html",
              "https://activetourism.it/viaggi/viaggi-in-programma/la-via-francigena-da-orvieto-e-roma",
              "https://adagioumbro.it/it/cosa-vedere-orvieto",
              "https://adventurebackpack.com/orvieto-itinerary",
              "https://adventuresinitaly.ca/2024/05/02/a-food-tour-of-orvieto",
              "https://adventuresinitaly.ca/2025/07/31/museums-of-orvieto",
              "https://airguide.info/the-5-best-restaurants-in-orvieto-italy",
              "https://aletes.it/torre-del-moro-nel-centro-di-orvieto",
              "https://alltrails.com/it-it/italy/umbria/orvieto",
              "https://altoadige.it/viaggiart/a-san-valentino-un-cammino-degli-innamorati-nella-orvieto-nascosta-bb8fj850",
              "https://altraopinione.org/museo/orvieto/pozzo-della-cava",
              "https://amicinvacanza.it/orvieto-con-bambini",
              "https://arezzonotizie.it/blog/arezzo-costume-e-societa/cappella-san-brizio-signorelli.html",
              "https://artribune.com/arti-visive/archeologia-arte-antica/2023/09/restauro-duomo-orvieto",
              "https://artsupp.com/it/orvieto/musei/museo-faina",
              "https://assoguide.it/it/st_location/le-nostre-citta/orvieto",
              "https://assovini.it/italia/umbria/item/525-orvieto-doc",
              "https://bellasboldadventures.com/complete-guide-to-orvieto-the-clifftop-city-of-umbria",
              "https://bellasboldadventures.com/gothic-masterpiece-complete-guide-to-orvieto-cathedrals-art-and-architecture",
              "https://bellaumbria.net/it/itinerari/cosa-vedere-a-orvieto-in-un-giorno",
              "https://blog.italiavirtualtour.it/cosa-vedere-a-orvieto-le-10-cose-da-visitare-in-un-giorno",
              "https://blog.ricksteves.com/blog/orvieto-cathedral-frescoes",
              "https://blog.vinbacco.com/cantine-a-orvieto-guida-completa-alle-migliori-visite-e-degustazioni-2026",
              "https://borghistoriciditalia.it/orvieto/orvieto-pozzo-di-san-patrizio",
              "https://cantineneri.it/esperienze",
              "https://cantineneri.it/orvieto-underground",
              "https://cartaunica.it",
              "https://cartaunica.it/cosa-vedere-ad-orvieto",
              "https://cartaunica.it/en/sotterranei-2",
              "https://cartaunica.it/sotterranei",
              "https://casalemanagement.com/2021/09/22/come-fare-un-tour-completo-di-orvieto-in-solo-mezza-giornata-dalla-cima-delle-torri-fino-alle-viscere-della-terra-nella-citta-dei-papi",
              "https://ceramichefravolini.com",
              "https://cesarine.com/en/experiences/orvieto-local-delicacies-food-wine-and-tradition-Y5rVfoEcIC",
              "https://chefdamiano.com/orvieto-a-timeless-gem-in-tuscanys-crown-of-culinary-and-cultural-delights",
              "https://ciaoumbria.it/cosa-vedere-a-orvieto",
              "https://cibotoday.it/mappe/ristoranti-dove-mangiare-orvieto.html",
              "https://cittadarteditalia.it/orvieto/torre-del-moro",
              "https://civitadibagnoregioguideturistiche.com/orvieto",
              "https://civitadibagnoregioguideturistiche.com/orvieto-il-palazzo-dei-sette-e-la-torre-del-moro",
              "https://cntraveler.com/activities/orvieto/torre-del-moro",
              "https://comune.orvieto.tr.it/it/news/146698/a-san-valentino-un-cammino-degli-innamorati-per-scoprire-i-segreti-della-citta-nascosta",
              "https://comune.orvieto.tr.it/it/topics/parcheggi",
              "https://comune.orvieto.tr.it/it/vivere/il-duomo-di-orvieto",
              "https://comune.orvieto.tr.it/it/vivere/pozzo-di-san-patrizio",
              "https://comune.orvieto.tr.it/it/vivere/torre-del-moro",
              "https://consigliviaggio.it/italia/orvieto-in-1-giorno-itinerario.html",
              "https://conventosanbartolomeo.com/un-giorno-a-orvieto-alla-scoperta-della-citta-sospesa-tra-cielo-e-tufo",
              "https://coopculture.it/en/poi/pozzo-di-san-patrizio",
              "https://corbula.it/itinerari-e-luoghi/cosa-vedere-a-orvieto",
              "https://corrierenerd.it/pozzo-di-san-patrizio",
              "https://cosafarei.it/orvieto",
              "https://cuisinevoila.com/orvieto-fine-dining",
              "https://cuisinevoila.com/orvieto-local-cuisine",
              "https://cuisinevoila.com/orvieto-street-food",
              "https://cuisinevoila.com/orvieto-traditional-recipes",
              "https://cultura.gov.it/luogo/orvieto-underground",
              "https://daichepartiamo.com/cosa-vedere-ad-orvieto-in-un-giorno",
              "https://decugnanodeibarbi.com/it",
              "https://decugnanodeibarbi.com/it/degustazioni-orvieto",
              "https://destinazioneorvieto.com",
              "https://destinazioneorvieto.com/arte-cultura/duomo-di-orvieto",
              "https://destinazioneorvieto.com/arte-cultura/museo-etrusco-claudio-faina",
              "https://destinazioneorvieto.com/arte-cultura/pozzo-della-cava",
              "https://destinazioneorvieto.com/categoria-acquistare/ceramiche-e-affreschi",
              "https://diventaunviaggiatore.it/cosa-vedere-a-orvieto-e-cosa-fare-nei-dintorni",
              "https://domenicafiore.com/orvieto/what-to-see",
              "https://doveparcheggiare.com/a-orvieto",
              "https://dreamplanexperience.com/day-trip-to-orvieto-from-rome",
              "https://dreavel.com/ita/4/attivita/175/orvieto-orvieto-underground",
              "https://duomodiorvieto.eu/duomo-di-orvieto/la-facciata",
              "https://duomodiorvieto.eu/en/claudio-faina-etruscan-museum",
              "https://duomodiorvieto.it",
              "https://duomodiorvieto.it/duomo-di-orvieto/la-cappella-di-san-brizio",
              "https://duomodiorvieto.it/duomo-di-orvieto/la-facciata",
              "https://duomodiorvieto.it/en",
              "https://duomodiorvieto.it/en/claudio-faina-etruscan-museum",
              "https://duomodiorvieto.it/en/duomo-di-orvieto/the-chapel-of-san-brizio",
              "https://duomodiorvieto.it/en/duomo-of-orvieto",
              "https://duomodiorvieto.it/en/duomo-of-orvieto/the-chapel-of-the-corporal",
              "https://duomodiorvieto.it/en/tickets",
              "https://duomodiorvieto.it/en/visit/plan-your-visit",
              "https://duomodiorvieto.it/museo-etrusco-claudio-faina",
              "https://duomodiorvieto.it/opera-del-duomo/la-storia",
              "https://duomoorvieto.it/duomo-di-orvieto",
              "https://duomoorvieto.it/duomo-di-orvieto/la-cappella-di-san-brizio",
              "https://duomoorvieto.it/visita/organizza-la-visita",
              "https://earthlymission.com/orvieto-underground-city-italy-discovery",
              "https://earthtrekkers.com/best-things-to-do-in-orvieto-italy",
              "https://eatweekguide.com/coro-orvieto",
              "https://eleonorafioravante.com/cosa-vedere-ad-orvieto-in-un-giorno",
              "https://elledecor.com/it/viaggi/a38448380/pozzo-di-san-patrizio-orvieto-storia-leggenda",
              "https://elledecor.com/it/viaggi/a38537896/orvieto-underground-citta-sotterranea-umbra",
              "https://emotionrit.it/2018/07/umbria-orvieto-in-un-giorno-cosa-vedere-e-cosa-non-perdere.html",
              "https://en.parkopedia.com/parking/orvieto",
              "https://en.parkopedia.it/parking/carpark/piazza_cahen/05018/orvieto",
              "https://esperienzaveraorvieto.com",
              "https://etruschi.name/orvieto/tempio_belvedere_it.html",
              "https://exploring-umbria.com/attivita/visita-guidata-di-orvieto",
              "https://exploring-umbria.com/en/discover-orvieto/discover-orvieto/tempio-tempio-del-belvedere-and-fanum-voltumnae",
              "https://exploring-umbria.com/en/discover-orvieto/eating-in-orvieto",
              "https://exploring-umbria.com/en/the-ceramics-in-umbria/orvieto",
              "https://exploring-umbria.com/la-ceramica-in-umbria/la-ceramica-di-orvieto",
              "https://exploring-umbria.com/scopri-orvieto-la-citta-narrante/prodotti-tipici-di-orvieto",
              "https://famigliainfuga.com/visitare-orvieto-in-1-giorno-cosa-vedere",
              "https://familygo.eu/vacanze-con-bambini/destinazioni/europa/italia/umbria/orvieto",
              "https://familygo.eu/vacanze-con-bambini/viaggi/orvieto/orvieto-con-bambini-cosa-vedere",
              "https://familygo.eu/vacanze-con-bambini/viaggi/orvieto/orvieto-underground-con-bambini-citta-sotterranea",
              "https://festivation.com/orvieto-local-traditions",
              "https://festivation.com/orvieto-traditional-food-guide",
              "https://finestrainformativa.com/biblioteca/articolo/read/19816-dove-si-parcheggia-ad-orvieto",
              "https://finestresullarte.info/en/works-and-artists/dante-and-orvieto-from-luca-signorelli-to-federico-zuccari",
              "https://finestresullarte.info/opere-e-artisti/luca-signorelli-affreschi-cappella-di-san-brizio-duomo-orvieto",
              "https://finestresullarte.info/viaggi/orvieto-cosa-vedere-10-luoghi-da-non-perdere",
              "https://flashpackingitaly.com/day-trip-from-rome-to-orvieto-by-train",
              "https://followingtherivera.com/1-day-in-orvieto-italy",
              "https://foodeviaggi.it/visitare-orvieto-in-poche-ore-il-meglio-da-non-perdere",
              "https://freedome.it/attivita/degustazione-vini-visita-cantina-storica-orvieto",
              "https://fulltravel.it/localita/orvieto",
              "https://gamberorosso.it/notizie/rubriche/itinerari/i-migliori-ristoranti-di-orvieto-dove-mangiare-in-citta",
              "https://getnofilter.com/en/spot/resti-del-tempio-etrusco-del-belvedere-in-orvieto-italy",
              "https://gigigriffis.com/ask-a-local-what-should-i-do-see-and-eat-in-orvieto-italy",
              "https://glimpses-of-the-world.com/2023/02/19/orvieto-things-to-do",
              "https://goticomania.it/gotico-italiano/duomo-di-orvieto.html",
              "https://gpsmycity.com/attractions/museo-claudio-faina-(claudio-faina-museum)-62657.html",
              "https://grownuptravels.com/best-things-to-do-in-orvieto-italy",
              "https://guidaturisticaorvieto.it",
              "https://guidaturisticaorvieto.it/proposte-di-viaggio",
              "https://guidaviterbo.com/da-visitare-vicino-ad-orvieto",
              "https://guidaviterbo.com/i-10-top-vicino-a-viterbo",
              "https://guidaviterbo.com/orvieto-litinerario-che-vuoi",
              "https://guide.michelin.com/ca/en/umbria/orvieto/restaurants",
              "https://guide.michelin.com/ch/it/umbria/orvieto/ristoranti",
              "https://guide.michelin.com/en/it/umbria/orvieto/restaurants",
              "https://guide.michelin.com/us/en/umbria/orvieto/restaurants",
              "https://haltadefinizione.com/en/viewer/work/san-brizio-chapel-luca-signorelli",
              "https://haltadefinizione.com/visualizzatore/opera/cappella-di-san-brizio-luca-signorelli",
              "https://handluggageonly.co.uk/orvieto-the-most-dramatic-city-in-europe",
              "https://hellotaste.it/vino/cantine/umbria/terni/orvieto",
              "https://hermesmagazine.it/article/experience/cose-fare-vedere-orvieto",
              "https://hotelkristallorvieto.it/dintorni",
              "https://hotelpostaorvieto.it/en/week-end-orvieto-cosa-vedere",
              "https://hotelpostaorvieto.it/week-end-orvieto-cosa-vedere",
              "https://humanalens.com/post/orvieto-cathedral-in-umbria",
              "https://ilgiardinodagiovanni.it",
              "https://ilgiornale.it/news/litalia-scoprire/orvieto-storia-pozzo-san-patrizio-2011961.html",
              "https://ilmessaggero.it/umbria/orvieto_duomo_signorelli_digitalizzato_affreschi-7561788.html",
              "https://ilmondodifra.com/dove-mangiare-a-orvieto-umbria",
              "https://ilturista.info/da-vedere/duomo-orvieto-storia-facciata-affreschi-interno",
              "https://in3giorni.com/faq/come-raggiungere-il-centro-di-orvieto-dalla-stazione",
              "https://in3giorni.com/faq/dove-parcheggiare-gratuitamente-a-orvieto",
              "https://in3giorni.com/faq/dove-parcheggiare-gratuitamente-ad-orvieto",
              "https://in3giorni.com/faq/dove-parcheggiare-orvieto-gratis",
              "https://in3giorni.com/faq/dove-posso-parcheggiare-gratuitamente-ad-orvieto",
              "https://in3giorni.com/faq/dove-posso-trovare-parcheggi-gratuiti-ad-orvieto",
              "https://in3giorni.com/faq/quanto-dura-la-visita-di-orvieto-sotterranea",
              "https://informagiovani-italia.com/dove_mangiare_orvieto.htm",
              "https://inviaggio.touringclub.it/consigli-di-viaggio/cosa-vedere-a-orvieto-su-e-giu-dalla-rupe-di-tufo",
              "https://inviaggioconmadi.it/cosa-vedere-a-orvieto-in-un-giorno",
              "https://invitationtotuscany.com/guide/italy/umbria/terni/a-day-in-orvieto",
              "https://ioamoiviaggi.it/orvieto-lumbria-jazz-festival",
              "https://it.wikiloc.com/percorsi-escursionismo/centro-storico-orvieto-e-anello-della-rupe-210000555",
              "https://it.wikiloc.com/percorsi-escursionismo/orvieto-duomo-palazzo-piccolomini-e-torre-del-moro-da-orvieto-186482375",
              "https://italia-italy.org/G148-1-turismo-umbria/cosa-vedere-dintorni-orvieto",
              "https://italia-italy.org/G148-1-turismo-umbria/luoghi-di-interesse-dintorni-orvieto",
              "https://italia-italy.org/en/G148-1-turismo-umbria/cosa-vedere-dintorni-orvieto",
              "https://italia-italy.org/en/G148-turismo-umbria-cosa-vedere-vicino-orvieto",
              "https://italia-italy.org/loc43833-musei-orvieto-museo-faina",
              "https://italia-italy.org/umbria-turismo-G148-orvieto",
              "https://italia.it/en/umbria/orvieto/cultural-places/orvieto-underground",
              "https://italia.it/it/umbria/orvieto/luoghi-della-cultura/pozzo-di-san-patrizio",
              "https://italiainblu.it/orvieto.html",
              "https://italianwinetour.info/cinque-cantine-che-possono-cambiare-la-storia-di-orvieto",
              "https://italiavai.com/umbria/10-cosa-vedere-orvierto.htm",
              "https://italiavibes.com/blog/a-photographers-guide-to-orvieto-best-viewpoints-golden-hour-tips-and-composition-secrets-for-capturing-the-city-and-surrounding-landscape",
              "https://italiavibes.com/blog/inside-orvietos-duomo-a-visitors-guide-to-the-cathedrals-art-architecture-and-the-chapel-of-san-brizio",
              "https://italiavibes.com/blog/orvieto-on-a-plate-a-food-lovers-guide-to-markets-trattorias-and-traditional-umbrian-dishes",
              "https://italien.expert/en/top-10-places-to-visit-in-orvieto",
              "https://italyfinds.com/one-day-in-orvieto-itinerary",
              "https://italyformovies.it/location/detail/18452",
              "https://italyguides.it/en/umbria/orvieto",
              "https://italyguides.it/en/umbria/orvieto/pozzo-di-san-patrizio",
              "https://italyguides.it/it/umbria/orvieto/torre-del-moro",
              "https://italynotes.com/stories/duomo-di-orvieto",
              "https://italyonair.com/luoghi/borghi/orvieto-cosa-vedere.html",
              "https://italyonair.com/luoghi/monumenti-e-attrazioni/pozzo-san-patrizio-orvieto-guida.html",
              "https://italyreview.com/orvieto.html",
              "https://italyscapes.com/places/umbria/orvieto/towers/torre-del-moro",
              "https://italytripper.com/en/place/orvieto-cathedral-gothic-masterpiece",
              "https://italytripper.com/en/place/orvieto-cathedral-gothic-masterpiece-2",
              "https://italytripper.com/it/place/duomo-di-orvieto-capolavoro-gotico",
              "https://italywineries.it/cantine-ad-orvieto",
              "https://italywineries.it/degustazioni-vini-orvieto",
              "https://italyyoudontexpect.com/dove-parcheggiare-a-orvieto",
              "https://itimaker.com/blog/best-restaurants-in-orvieto",
              "https://itimaker.com/blog/pozzo-di-san-patrizio-orvieto",
              "https://itinerarieluoghi.it/dintorni-di-orvieto-tre-luoghi-da-non-perdere",
              "https://itravelforthestars.com/orvieto-travel-guide",
              "https://journeystoitaly.com/discover-st-patricks-well-in-orvieto",
              "https://keytoumbria.com/Orvieto/Museo_Claudio_Faina.html",
              "https://kidpass.it/orvieto-con-i-bambini-la-nostra-giornata-in-umbria-in-famiglia",
              "https://knownode.blog/perfect-day-orvieto-walking-map",
              "https://komoot.com/guide/607912/attractions-around-orvieto",
              "https://komoot.com/it-it/guide/607906/fantastici-trekking-e-escursioni-nei-dintorni-di-orvieto",
              "https://komoot.com/it-it/guide/607912/cosa-da-vedere-e-attrazioni-nei-dintorni-di-orvieto",
              "https://lebellezzedellostivale.com/weekend-a-orvieto-cosa-fare-in-due-giorni",
              "https://lecosmopolite.it/cose-da-vedere-a-orvieto-in-un-giorno",
              "https://lecosmopolite.it/pozzo-di-san-patrizio-a-orvieto",
              "https://lessormorgan.com/2018/09/29/why-orvieto-is-a-small-town-in-italy-is-worth-a-visit",
              "https://lifepart2andbeyond.com/orvieto-day-trip-from-rome",
              "https://lifestylemadeinitaly.it/cosa-vedere-ad-orvieto-e-dintorni",
              "https://liveorvieto.com/blog/monumenti/orvieto-underground",
              "https://liveorvieto.com/blog/monumenti/pozzo-di-san-patrizio",
              "https://liveorvieto.com/blog/monumenti/tempio-del-belvedere",
              "https://liveorvieto.com/blog/proposte_di_viaggio/dentro-la-rupe",
              "https://liveorvieto.com/calendario-eventi",
              "https://liveorvieto.com/en/blog/monumenti/la-torre-del-moro",
              "https://liveorvieto.com/en/blog/monumenti/museo-civico-ed-etrusco-claudio-faina",
              "https://liveorvieto.com/en/blog/monumenti/pozzo-di-san-patrizio",
              "https://liveorvieto.com/en/blog/monumenti/tempio-del-belvedere",
              "https://liveorvieto.com/informazioni-utili",
              "https://liveorvieto.com/la-tradizione-artigiana",
              "https://livetheworld.com/activities/italy/duomo-di-orvieto",
              "https://locandaorvieto.it",
              "https://locandaorvieto.it/en",
              "https://locandaorvieto.it/en/piatti-tipici-di-orvieto",
              "https://lonelyplanet.com/destinations/italy/umbria-and-le-marche/orvieto",
              "https://lonelyplanetitalia.it/articoli/idee-per-il-weekend/visitare-orvieto",
              "https://lorenzotaccioli.it/orvieto-in-un-giorno-cosa-vedere",
              "https://love-umbria.com/tour/orvieto-walking-tour",
              "https://lunediacolazione.it/2024/10/12/visitare-orvieto-in-un-giorno-itinerario-a-piedi",
              "https://madonnadellatte.it/it/visite-e-degustazioni",
              "https://mamalovesitaly.com/one-day-in-orvieto-italy",
              "https://mediasetinfinity.mediaset.it/video/freedomoltreilconfine/orvieto-il-pozzo-di-san-patrizio_F310071101008C14",
              "https://meteweekend.it/luoghi/orvieto-in-un-weekend",
              "https://minimalglobetrotter.com/2026/02/10/san-valentino-a-orvieto-un-cammino-damore",
              "https://mominitaly.com/orvieto-with-kids",
              "https://mosaicus.com/terni/visite-guidate-terni/orvieto-sotterranea",
              "https://msn.com/it-it/viaggi/notizie/scoprire-il-fascino-primaverile-di-orvieto-in-tre-giorni/ar-AA1VRAbc",
              "https://museofaina.it",
              "https://museofaina.it/chi-siamo-fondazione-museo-claudio-faina",
              "https://museofaina.it/collezione-museo-faina",
              "https://museofaina.it/collezione-museo-faina/gioielli-e-monete",
              "https://museofaina.it/collezione-museo-faina/il-museo-dei-ragazzi",
              "https://museofaina.it/collezione-museo-faina/museo-civico-archeologico",
              "https://museofaina.it/collezione-museo-faina/museo-etrusco-claudio-faina",
              "https://museofaina.it/contatti-dove-siamo",
              "https://museofaina.it/news-fondazione-museo-faina",
              "https://museofaina.it/palazzo-faina",
              "https://museofaina.it/partnership",
              "https://myadventuresacrosstheworld.com/orvieto-italy-things-to-do",
              "https://mydailyartdisplay.uk/tag/orvieto-cathedral",
              "https://mytravelaffairs.com/destinations/orvieto-what-see-where-eat-and-what-do-evening",
              "https://nationaltraveller.com/orvieto-what-to-see-where-to-eat-and-what-to-do-in-the-evening",
              "https://offseasontrip.it/orvieto-cosa-vedere-tutti-i-consigli",
              "https://oggidoveandiamo.com/cosa-vedere-a-orvieto",
              "https://omniadigitale.it/leggenda-e-storia-del-pozzo-di-san-patrizio-a-orvieto",
              "https://omniaforitaly.org/en/cards/st-patrick-s-well-skip-the-line-entrance",
              "https://opentable.com/landmark/restaurants-near-orvieto",
              "https://opentable.it/landmark/restaurants-near-duomo-orvieto",
              "https://opentable.it/landmark/restaurants-near-orvieto",
              "https://orvietart.it",
              "https://orvietart.it/en/home",
              "https://orvieto.com/tour/orvieto-underground-tour",
              "https://orvieto.com/walking-tour",
              "https://orvietocosavedere.it",
              "https://orvietocosavedere.it/cosa-vedere-a-orvieto-in-un-giorno",
              "https://orvietoduomo.it",
              "https://orvietoduomo.it/cappella-san-brizio",
              "https://orvietoduomo.it/facciata-duomo-orvieto",
              "https://orvietoinvacanza.it/cosa-vedere/orvieto-underground",
              "https://orvietoinvacanza.it/cosa-vedere/torre-del-moro",
              "https://orvietonews.it/cultura/2021/09/21/ricostruzione-3d-del-tempio-del-belvedere-al-museo-archeologico-nazionale-89257.html",
              "https://orvietonotizie.it/un-nuova-guida-per-il-museo-claudio-faina",
              "https://orvietoorbust.com/2015/08/28/31-days-of-orvieto-day-28-coffeecaffe",
              "https://orvietosi.it/2026/02/a-san-valentino-un-cammino-degli-innamorati-per-scoprire-i-segreti-della-citta-nascosta",
              "https://orvietounderground.it",
              "https://orvietounderground.it/contattiinfo",
              "https://orvietounderground.it/en/contactsinfo",
              "https://orvietounderground.it/en/home",
              "https://orvietounderground.it/en/the-discovery",
              "https://orvietoviva.com",
              "https://orvietoviva.com/arrivare-e-muoversi-ad-orvieto",
              "https://orvietoviva.com/artigianato-orvieto",
              "https://orvietoviva.com/attivita-ed-escursioni",
              "https://orvietoviva.com/bar-montanucci",
              "https://orvietoviva.com/category/artigianato-ceramica-orvieto",
              "https://orvietoviva.com/category/degustazioni-cantine",
              "https://orvietoviva.com/conoscere-orvieto",
              "https://orvietoviva.com/cosa-fare-a-orvieto",
              "https://orvietoviva.com/cosa-vedere-orvieto",
              "https://orvietoviva.com/cucina-tipica-orvietana",
              "https://orvietoviva.com/degustazioni-in-cantina",
              "https://orvietoviva.com/domande-frequenti-per-visitare-orvieto",
              "https://orvietoviva.com/dormire-orvieto",
              "https://orvietoviva.com/dove-mangiare-orvieto",
              "https://orvietoviva.com/duomo-orvieto",
              "https://orvietoviva.com/en",
              "https://orvietoviva.com/en/about-orvieto",
              "https://orvietoviva.com/en/accommodations-orvieto",
              "https://orvietoviva.com/en/around-orvieto",
              "https://orvietoviva.com/en/bar-montanucci-orvieto",
              "https://orvietoviva.com/en/category/orvieto-ceramic-shops",
              "https://orvietoviva.com/en/ceramic-orvieto",
              "https://orvietoviva.com/en/do-in-orvieto",
              "https://orvietoviva.com/en/etruscan-tempio",
              "https://orvietoviva.com/en/events-in-orvieto",
              "https://orvietoviva.com/en/frequently-asked-questions-about-visiting-orvieto",
              "https://orvietoviva.com/en/getting-moving-orvieto",
              "https://orvietoviva.com/en/guided-tour-duomo-masterpieces",
              "https://orvietoviva.com/en/museo-faina-orvieto",
              "https://orvietoviva.com/en/orvieto-artisan-traditions",
              "https://orvietoviva.com/en/orvieto-history",
              "https://orvietoviva.com/en/orvieto-main-sights",
              "https://orvietoviva.com/en/orvieto-parking",
              "https://orvietoviva.com/en/orvieto-restaurants",
              "https://orvietoviva.com/en/orvieto-restaurants-trattorias",
              "https://orvietoviva.com/en/orvieto-shopping",
              "https://orvietoviva.com/en/orvieto-typical-foods",
              "https://orvietoviva.com/en/orvieto-underground",
              "https://orvietoviva.com/en/orvieto-what-to-see",
              "https://orvietoviva.com/en/pastry-shops-orvieto",
              "https://orvietoviva.com/en/pozzo-di-san-patrizio",
              "https://orvietoviva.com/en/the-duomo-of-orvieto",
              "https://orvietoviva.com/en/the-underworld-of-orvieto",
              "https://orvietoviva.com/en/torre-del-moro",
              "https://orvietoviva.com/en/traditional-cuisine-of-orvieto",
              "https://orvietoviva.com/eventi-e-feste-ad-orvieto",
              "https://orvietoviva.com/eventi-orvieto",
              "https://orvietoviva.com/funicolare-e-bus-orvieto",
              "https://orvietoviva.com/gelaterie-orvieto",
              "https://orvietoviva.com/itinerari-a-piedi-orvieto",
              "https://orvietoviva.com/itinerari-auto-orvieto",
              "https://orvietoviva.com/itinerari-per-famiglie-ad-orvieto-e-dintorni",
              "https://orvietoviva.com/la-bottega-del-pozzo",
              "https://orvietoviva.com/mappa-orvieto",
              "https://orvietoviva.com/musei-archeologici-civico-e-faina",
              "https://orvietoviva.com/negozi-orvieto",
              "https://orvietoviva.com/orvieto-con-bambini",
              "https://orvietoviva.com/orvieto-natura",
              "https://orvietoviva.com/orvieto-underground",
              "https://orvietoviva.com/parcheggi-ad-orvieto",
              "https://orvietoviva.com/pozzo-della-cava",
              "https://orvietoviva.com/pozzo-di-san-patrizio",
              "https://orvietoviva.com/ristoranti-orvieto",
              "https://orvietoviva.com/storia-orvieto",
              "https://orvietoviva.com/tempio-del-belvedere",
              "https://orvietoviva.com/territorio",
              "https://orvietoviva.com/torre-del-moro",
              "https://orvietoviva.com/tour-guidato-orvieto-nascosta",
              "https://orvietoviva.com/tour-sottosuolo-di-orvieto",
              "https://orvietoviva.com/tours-orvieto",
              "https://orvietoviva.com/turismo-orvieto",
              "https://ostedelreorvieto.com",
              "https://osteriadelgrilloorvieto.it",
              "https://ourescapeclause.com/things-to-do-in-orvieto-italy",
              "https://paesidelgusto.it/e-la-porta-sevvero-una-regione-di-gusto-5-indirizzi-dove-mangiare-a-orvieto",
              "https://palazzone.com/degustazioni-palazzone",
              "https://palazzopetrvs.com/attivita",
              "https://palazzopiccolomini.it/orvieto-e-dintorni-5-tappe-imperdibili-da-scoprire",
              "https://parkme.com/orvieto-it-parking",
              "https://parkopedia.it/parcheggi/stazione/orvieto",
              "https://passaportodifamiglia.com/en/italy/umbria-en/visit-pozzo-san-patrizio-children",
              "https://passaportodifamiglia.com/italia/umbria/visita-citta-orvieto-dintorni-bambini",
              "https://passaportodifamiglia.com/italia/umbria/visita-pozzo-cava-bambini",
              "https://picturethispost.com/orvieto-underground-dovecote-central",
              "https://pimpmytrip.it/cosa-vedere-a-orvieto",
              "https://poracciintour.com/cosa-vedere-a-orvieto-in-un-giorno",
              "https://postcard.inc/places/torre-del-moro-orvieto-orvieto-Q118-9w54Ci",
              "https://pottypadre.com/luca-signorelli-the-chapel-of-san-brizio-orvieto",
              "https://powertraveller.com/orvieto-guided-walking-tour-of-underground-orvieto",
              "https://pozzodellacava.it",
              "https://pozzodellacava.it/bottega/il-laboratorio",
              "https://pozzodisanpatrizio.it",
              "https://pozzodisanpatrizio.it/en/home-english",
              "https://pozzodisanpatrizio.it/en/hours-tickets-st-patricks-well",
              "https://pozzodisanpatrizio.it/la-storia-del-pozzo-di-san-patrizio",
              "https://pretapartirconchiara.com/itinerario-a-piedi-per-orvieto-cosa-vedere-in-un-giorno",
              "https://progettosigillo.it/cosa-vedere-a-orvieto-scopri-i-tesori-nascosti-di-una-citta-incantata",
              "https://prontivaligiaevia.com/2020/11/06/orvieto-cosa-vedere-con-i-bambini-in-poche-ore",
              "https://quantomanca.com/orvieto-cosa-vedere-con-i-bambini",
              "https://realumbria.it/26/01/2023/il-pozzo-di-san-patrizio-simboli-e-visioni",
              "https://recordrentacar.com/en/what-see-orvieto",
              "https://reneedevoemertz.com/visual-writer/2020/4/28/the-great-indoors-duomo-di-orvieto",
              "https://restaurantguru.it/pastry-Orvieto-m32",
              "https://ricksteves.com/europe/italy/orvieto",
              "https://ricksteves.com/watch-read-listen/read/articles/orvieto-what-an-italian-hill-town-should-be",
              "https://ristorante-maurizio.com/en/the-restaurant",
              "https://rivogliolabarbie.com/2023/04/23/centro-storico-orvieto-cosa-vedere-in-un-giorno",
              "https://roadmindtrip.com/cosa-vedere-a-orvieto-guida-per-un-weekend",
              "https://roadscholar.org/blog/discover-orvieto-etruscan-caves-of-italy",
              "https://roamaround.app/itinerary/orvieto/1-days/64bb7bac4a3eb4bb6a379920",
              "https://roccantica.it/places/detail/resti-del-tempio-etrusco-del-belvedere/umbria/orvieto/museum/ChIJ3dSxV6rNLhMR8SwFu-LgmeI",
              "https://romaweekend.it/articoli/orvieto-cosa-vedere-dove-mangiare",
              "https://saliinvetta.com/culture-e-tradizioni/2484-la-leggenda-del-pozzo-di-san-patrizio",
              "https://saltinourhair.com/italy/orvieto",
              "https://sangeminitravelhub.com/cosa-vedere-orvieto-in-un-giorno",
              "https://santamariadegliancillotti.com/it/blog/cosa-vedere-a-orvieto-un-viaggio-tra-le-meraviglie-della-citta-293",
              "https://santamariadegliancillotti.com/it/blog/il-duomo-di-orvieto-storia-arte-e-spiritualita-319",
              "https://saturdaysinrome.com/blog/where-to-eat-in-orvieto",
              "https://savoringitaly.com/orvieto-italy",
              "https://scoprilumbria.com/visita-le-citta-umbre/orvieto.html",
              "https://secretlocale.com/hidden-orvieto-restaurants-locals",
              "https://secretlocale.com/orvieto-best-quiet-cafes",
              "https://secretlocale.com/orvieto-hidden-artisan-shops",
              "https://secretlocale.com/orvieto-historic-caves-tour-booking",
              "https://secretlocale.com/orvieto-locals-favorite-spots",
              "https://secretumbria.it/cosa-vedere-in-umbria/orvieto",
              "https://secretumbria.it/duomo-orvieto-santa-maria-assunta",
              "https://secretumbria.it/en/moros-tower-palace-of-the-seven-peoples-palace",
              "https://secretumbria.it/en/orvieto-underground",
              "https://secretumbria.it/en/the-well-of-saint-patrick",
              "https://secretumbria.it/orvieto-sotterranea",
              "https://secretumbria.it/pozzo-san-patrizio-orvieto",
              "https://secretumbria.it/torre-moro-palazzo-sette-popolo-orvieto",
              "https://secretumbria.it/tour-a-piedi-di-orvieto",
              "https://shop.cantineneri.it/prodotto/lungo-la-via-francigena",
              "https://siviaggia.it/idee-di-viaggio/cosa-vedere-orvieto-uno-borghi-belli-umbria/189055",
              "https://siviaggia.it/idee-di-viaggio/duomo-orvieto/539263",
              "https://siviaggia.it/informazioni-utili/cosa-conviene-comprare-a-orvieto/155537",
              "https://slowfoodtravelers.com/city-guides/orvieto",
              "https://slowtraveltours.com/blog/food-tour-of-orvieto",
              "https://souvenirdiviaggio.it/cosa-vedere-a-orvieto-in-un-giorno-itinerario",
              "https://spa-advisor.it/orvieto-cosa-vedere",
              "https://sportoutdoor24.it/viaggi/italia/pozzo-di-san-patrizio-orvieto",
              "https://storieurbane.it/il-pozzo-di-san-patrizio-tra-storia-mistero-e-ingegneria",
              "https://strictlyitaly.com/things-to-do-in-orvieto-italy",
              "https://suitcaseescapes.com/things-to-do-orvieto-italy",
              "https://sviaggiare.it/umbria/orvieto-cose-da-vedere",
              "https://tendenzediviaggio.it/weekend-a-orvieto-cosa-vedere-e-quanto-costa-un-soggiorno-nella-perla-dellumbria",
              "https://theculturetrip.com/europe/italy/articles/the-top-5-things-to-do-and-see-in-orvieto",
              "https://theculturetrip.com/europe/italy/orvieto/articles/the-10-best-restaurants-in-orvieto",
              "https://theitalyedit.com/italy/articles/the-10-best-restaurants-in-orvieto",
              "https://theflashpacker.net/things-to-do-in-orvieto-italy-guide",
              "https://thegeographicalcure.com/post/guide-orvieto-cathedral",
              "https://thegeographicalcure.com/post/one-day-in-orvieto",
              "https://thegeographicalcure.com/post/san-brizio-chapel-orvieto",
              "https://theguardian.com/travel/2023/may/14/the-captivating-hilltop-city-of-orvieto",
              "https://theitalyedit.com/36-hours-in-orvieto-umbria",
              "https://thetouristchecklist.com/things-to-do-in-orvieto",
              "https://thewanderlustrose.com/a-complete-guide-to-orvieto-the-hidden-gem-of-italy",
              "https://thewanderlustrose.com/how-to-visit-orvieto-italy-on-a-budget",
              "https://thewomtravel.com/italy/umbria/st-patrick-well.html",
              "https://thewonderinglotuslifestyle.com/visiting-orvieto-in-one-day",
              "https://tipicamenteumbria.it/ad-orvieto-la-ceramica-si-veste-di-storia",
              "https://tipintravel.com/orvieto-cose-da-vedere",
              "https://tiraccontounviaggio.it/cosa-vedere-a-orvieto-in-un-giorno-itinerario-e-luoghi-principali-da-visitare",
              "https://to-tuscany.com/travel-guide/towns-villages/orvieto",
              "https://tourismattractions.net/italy/best-orvieto-viewpoints",
              "https://touristainitalia.com/2020/10/26/orvieto-un-trekking-cittadino-nella-citta-alta-e-strana",
              "https://touristattractions.travel/it/s/italy/orvieto/online-guide",
              "https://trattoriadelmoro.info",
              "https://trattoriadelmoro.info/en",
              "https://travel.thewom.it/italia/umbria/pozzo-di-san-patrizio.html",
              "https://travel365.it/orvieto-cosa-vedere.htm",
              "https://travelerbibles.com/places-to-visit-near-orvieto",
              "https://travelife.it/cosa-vedere-ad-orvieto",
              "https://travelingintuscany.com/art/lucasignorelli/sanbriziochapel.htm",
              "https://travelmag.com/restaurants-bars-orvieto-italy",
              "https://travelsewhere.net/sightseeing-orvieto-italy",
              "https://traveltoitalyguide.com/whats-near-orvieto",
              "https://travelworld.it/visitare-orvieto-2-giorni-per-scoprire-la-perla-dellumbria",
              "https://trevaligie.com/orvieto-cosa-vedere-in-1-giorno",
              "https://tripbase.com/destinations/orvieto-italy/food",
              "https://trips2italy.com/umbria/orvieto/food-wine-t2i",
              "https://turistipercaso.it/consigli-e-itinerari-di-viaggio/orvieto",
              "https://turistipercaso.it/destinazioni/62-metri-nel-cuore-della-terra-con-una-doppia-elica-geniale-il-pozzo-magico-dove-non-ci-si-incontra-mai.html",
              "https://tuscanynowandmore.com/discover-italy/historic-cities/visit-orvieto-travel-guide",
              "https://tuscanyumbriablog.com/around-fontanaro-restaurant-review/our-best-restaurants-in-orvieto",
              "https://tuscanyumbriablog.com/cultural-tours/discover-orvieto-top-restaurants-activities-and-historical-insights",
              "https://umbria.tag24.it/cosa-fare-e-vedere-a-orvieto-in-un-giorno",
              "https://umbriaabove.com/parking-and-getting-around-in-orvieto-umbria",
              "https://umbriaabove.com/visitor-perspective-by-palazzo-misciattelli-6-the-pozzo-della-cava",
              "https://umbriabimbo.it/tag/cosa-fare-con-bambini-ad-orvieto",
              "https://umbriabimbo.it/visitare-orvieto-bambini-guida-pratica-veloce-famiglia",
              "https://umbriaconme.com/en/news/orvieto-s-cathedral-and-the-chapel-of-saint-brizio_21.html",
              "https://umbriaconme.com/it/tours/esperienze-cosa-fare-in-umbria_31/tour-di-orvieto-underground-e-aperitivo-di-fronte-al-duomo_253.html",
              "https://umbriaconme.com/it/tours/guide-turistiche-e-visite-guidate-in-umbria_1/guida-turistica-orvieto_19.html",
              "https://umbriaconme.com/it/tours/guide-turistiche-e-visite-guidate-in-umbria_1/visitare-orvieto-con-la-guida_19.html",
              "https://umbriaecultura.it/luca-signorelli-cappella-san-brizio-orvieto",
              "https://umbriaonline.com/ceramica-orvieto.phtml",
              "https://umbriasi.it/pozzo-di-san-patrizio",
              "https://umbriatourism.it/en/w/archaeological-area-of-the-tempio-of-belvedere",
              "https://umbriatourism.it/en/w/claudio-faina-museum-and-civic-archaeological-museum-orvieto",
              "https://umbriatourism.it/en/w/st-patrick-s-well",
              "https://umbriatourism.it/en/w/the-ceramics-of-orvieto",
              "https://umbriatourism.it/en/w/the-chapel-of-saint-brizio-inside-orvieto-s-duomo",
              "https://umbriatourism.it/it/w/duomo-di-santa-maria-assunta-in-cielo-it",
              "https://umbriatourism.it/it/w/la-ceramica-di-orvieto",
              "https://umbriatourism.it/it/w/museo-claudio-faina-e-museo-civico-orvieto",
              "https://umbriatourism.it/it/w/orvieto-underground",
              "https://umbriatourism.it/it/w/pozzo-di-san-patrizio",
              "https://unafotografainviaggio.com/italia-orvieto",
              "https://viadifrancesco.it/en/web/umbria/w/the-chapel-of-saint-brizio-inside-orvieto-s-duomo",
              "https://viaggi.corriere.it/europa/italia/cards/orvieto",
              "https://viaggi.corriere.it/itinerari-e-luoghi/gallery/orvieto-segreta-nuovi-percorsi-per-nuove-esperienze-nella-natura",
              "https://viaggi.corriere.it/itinerari-e-luoghi/orvieto-che-sorpresa-tra-foreste-canyon-e-reperti-archeologici-la-esperienza-nuova-comune-orvieto-native-adv",
              "https://viaggiadipiu.it/2020/03/09/visitare-orvieto-informazioni-e-consigli-utili",
              "https://viaggiando-italia.it/cosa-visitare-a-orvieto-e-dintorni-guida-completa-per-3-giorni",
              "https://viaggiando-italia.it/en/st-patricks-well-in-orvieto-a-journey-through-italys-most-fascinating-underground-masterpiece",
              "https://viaggiando-italia.it/quali-sono-i-giorni-e-orari-di-apertura-del-pozzo-di-san-patrizio-a-orvieto",
              "https://viaggiareinumbria.com/10-cose-da-vedere-a-orvieto",
              "https://viaggiareinumbria.com/orvieto-in-famiglia-visita-con-bambini",
              "https://viaggiaremangiare.it/cosa-vedere-a-orvieto-in-un-week-end",
              "https://viaggiatorefelice.com/cosa-vedere-a-orvieto",
              "https://viaggiatricecuriosa.it/2023/11/02/cappella-di-san-brizio-orvieto-beato-angelico-luca-signorelli",
              "https://viaggieritratti.it/cosa-vedere-a-orvieto-e-dintorni",
              "https://viaggievacanze.com/pozzo-di-san-patrizio-18560.html",
              "https://viaggifuorirotta.it/pozzo-di-san-patrizio-leggenda",
              "https://viaggiomag.it/italia/pozzo-di-san-patrizio-orvieto-storia-leggenda",
              "https://vivielasciaviaggiare.com/2023/11/14/cosa-vedere-un-giorno-orvieto",
              "https://voloscontato.it/ristoranti/orvieto-dove-mangiare-bene-spendendo-poco.php",
              "https://walksofitaly.com/blog/things-to-do/is-orvieto-worth-visiting",
              "https://wandereatandtell.com/2020/07/10/orvieto-street-art-food-and-wine",
              "https://wanderlog.com/it/list/geoCategory/105091/le-migliori-cose-da-fare-e-attrazioni-a-orvieto",
              "https://wildtrips.net/citta/Orvieto-Italia-cosa-visitare.htm",
              "https://winalist.com/blog/italy-wine-region/umbria/orvieto-things-to-do",
              "https://winetourism.com/it/cantine-in-orvieto-classico",
              "https://wonderlustwanderings.com/2018/08/02/a-guide-to-orvieto-italy",
              "https://world-tourism.org/orvieto-2-hour-cathedral-and-underground-tour",
              "https://wsj.com/arts-culture/fine-art/luca-signorelli-san-brizio-chapel-orvieto-cathedral-italy-renaissance-792ef710",
              "https://yogawinetravel.com/visit-orvieto-in-italy-the-perfect-day-trip-if-you-want-to-avoid-romes-tourist-crowds"
          ]
          
          # Suddivisione matematica statica (Chunking) per performance ottimali
          for i in range(1, 7):
              chunk = TARGETS[i-1::6]
              with open(f"/tmp/step1_queue_{i}.json", "w") as f:
                  json.dump(chunk, f)
          print("Code distribuite Step 1 generate con successo.")
          EOF
          python -u m2_step1_setup.py

      - name: Creazione Modulo Condiviso - Pulizia Link e Conversione Markdown
        run: |
          cat << 'EOF' > extraction_utils.py
          """
          Modulo condiviso tra Step 1 e Step 2.
          Contiene SOLO logica di estrazione/pulizia: nessuna modifica a proxy,
          retry, validazione WAF o altra logica di scraping.

          Pulizia link basata esclusivamente su urllib (libreria standard, nessuna
          dipendenza aggiuntiva):
            1. Relativi -> assoluti (urljoin)
            2. Rimozione parametri di tracciamento dalla query string
            3. Rimozione ancore interne pure (#frammento sulla stessa pagina)
            4. Rimozione link a social/junk noti
            5. Rimozione link che puntano alla pagina stessa (self-link)
          Il testo pulito viene poi convertito in Markdown con markdownify,
          mantenendo i link superstiti nel formato [Testo](URL).
          """
          import re
          from urllib.parse import urljoin, urlsplit, urlunsplit, parse_qsl, urlencode

          from bs4 import BeautifulSoup
          from markdownify import markdownify as md_convert

          # Tag strutturali che non portano contenuto editoriale utile
          STRUCTURAL_TAGS_TO_STRIP = ["script", "style", "nav", "footer", "header",
                                      "form", "noscript", "iframe", "svg"]

          # Parametri di query da rimuovere sempre (tracciamento marketing/analytics)
          TRACKING_PARAMS_PREFIXES = ("utm_",)
          TRACKING_PARAMS_EXACT = {
              "fbclid", "gclid", "gclsrc", "dclid", "msclkid", "mc_cid", "mc_eid",
              "igshid", "ref", "ref_src", "ref_url", "source", "spm", "yclid",
              "_ga", "_gl", "cmpid", "campaign_id", "ad_id", "adgroup_id",
              "wbraid", "gbraid", "twclid", "vero_id", "trk", "trkCampaign",
              "share", "share_id", "si"
          }

          # Domini social/junk: i link verso questi domini vengono despogliati
          # (restano come testo semplice, non come link markdown)
          JUNK_LINK_DOMAINS = (
              "twitter.com", "x.com", "facebook.com", "fb.com", "instagram.com",
              "linkedin.com/share", "linkedin.com/shareArticle", "pinterest.com",
              "pinterest.it", "whatsapp.com", "wa.me", "t.me", "telegram.me",
              "reddit.com/submit", "tumblr.com/share", "getpocket.com",
              "addtoany.com", "sharethis.com", "mailto:"
          )


          def _is_junk_domain(netloc, full_href):
              netloc_low = netloc.lower()
              href_low = full_href.lower()
              for junk in JUNK_LINK_DOMAINS:
                  if junk in netloc_low or href_low.startswith(junk):
                      return True
              return False


          def _clean_query(query_string):
              if not query_string:
                  return ""
              pairs = parse_qsl(query_string, keep_blank_values=True)
              cleaned_pairs = []
              for key, value in pairs:
                  key_low = key.lower()
                  if key_low in TRACKING_PARAMS_EXACT:
                      continue
                  if any(key_low.startswith(pfx) for pfx in TRACKING_PARAMS_PREFIXES):
                      continue
                  cleaned_pairs.append((key, value))
              return urlencode(cleaned_pairs)


          def clean_link_href(raw_href, page_url):
              """
              Ritorna l'URL assoluto e ripulito, oppure None se il link va
              despogliato (ancora interna, self-link, social/junk, vuoto,
              javascript:, mailto: senza indirizzo valido).
              """
              if not raw_href:
                  return None

              raw_href = raw_href.strip()
              if not raw_href or raw_href.startswith("javascript:"):
                  return None
              if raw_href.startswith("mailto:") and len(raw_href) <= len("mailto:"):
                  return None

              # Assolutizza (gestisce anche i protocol-relative //host/path)
              try:
                  absolute = urljoin(page_url, raw_href)
              except Exception:
                  return None

              try:
                  parts = urlsplit(absolute)
              except Exception:
                  return None

              if parts.scheme not in ("http", "https", "mailto"):
                  return None

              if parts.scheme == "mailto":
                  return None

              # Ancora pura sulla stessa pagina (stesso path, solo #frammento)
              page_parts = urlsplit(page_url)
              same_path = (parts.netloc == page_parts.netloc and
                           parts.path.rstrip("/") == page_parts.path.rstrip("/"))

              if same_path and parts.fragment and not parts.query:
                  return None

              # Social / junk
              if _is_junk_domain(parts.netloc, absolute):
                  return None

              # Pulizia query string dai parametri di tracciamento
              cleaned_query = _clean_query(parts.query)

              # Ricostruzione URL: rimuove sempre il fragment (ancore interne)
              # a meno che non punti a una pagina diversa con un frammento
              # significativo (raro nei casi editoriali, quindi lo scartiamo
              # comunque per pulizia).
              rebuilt = urlunsplit((parts.scheme, parts.netloc, parts.path,
                                     cleaned_query, ""))

              # Self-link: stesso URL (dopo pulizia) della pagina sorgente
              page_rebuilt_parts = urlsplit(page_url)
              page_cleaned_query = _clean_query(page_rebuilt_parts.query)
              page_rebuilt = urlunsplit((page_rebuilt_parts.scheme,
                                          page_rebuilt_parts.netloc,
                                          page_rebuilt_parts.path.rstrip("/"),
                                          page_cleaned_query, ""))
              rebuilt_no_trailing = urlunsplit((parts.scheme, parts.netloc,
                                                 parts.path.rstrip("/"),
                                                 cleaned_query, ""))
              if rebuilt_no_trailing == page_rebuilt:
                  return None

              return rebuilt


          def _strip_anchor_tag(a_tag):
              """Sostituisce un <a> con il suo solo testo, senza rimuoverlo dal DOM."""
              a_tag.replace_with(a_tag.get_text())


          def html_to_clean_markdown(html, page_url):
              """
              Pipeline unica: rimuove tag strutturali, pulisce i link chirurgicamente
              con urllib, converte in Markdown con markdownify.
              Ritorna il markdown completo (nessun troncamento).
              """
              soup = BeautifulSoup(html, "html.parser")

              for tag_name in STRUCTURAL_TAGS_TO_STRIP:
                  for tag in soup.find_all(tag_name):
                      tag.decompose()

              for a_tag in soup.find_all("a"):
                  href = a_tag.get("href")
                  cleaned = clean_link_href(href, page_url)
                  if cleaned is None:
                      _strip_anchor_tag(a_tag)
                  else:
                      a_tag["href"] = cleaned
                      # Rimuove eventuali attributi di tracciamento superflui
                      for attr in list(a_tag.attrs):
                          if attr not in ("href", "title"):
                              del a_tag[attr]

              markdown_text = md_convert(
                  str(soup),
                  heading_style="ATX",
                  bullets="-",
                  strip=["img"],
              )

              # Collassa 3+ righe vuote consecutive in 2, pulisce i bordi
              markdown_text = re.sub(r"\n{3,}", "\n\n", markdown_text)
              markdown_text = markdown_text.strip()

              return markdown_text
          EOF

      - name: Creazione Modulo Condiviso - Estrazione Nugget via Gemini Live
        run: |
          cat << 'EOF' > nugget_extractor.py
          """
          Modulo condiviso tra Step 1 e Step 2 per l'estrazione di nugget strutturati
          tramite Gemini Live API (gemini-3.1-flash-live-preview).

          Segue lo stesso pattern del middleware filtro URL (url_screener.py):
            - client.aio.live.connect con response_modalities=["AUDIO"] +
              output_audio_transcription per ottenere testo dalla sessione Live
            - timeout di sicurezza con asyncio.wait_for
            - parsing JSON robusto con fallback su regex ```json ... ```

          Principio non-negoziabile (coerente con ARIA):
            la "confidence" NON e' mai auto-riportata dal modello. Viene derivata
            deterministicamente confrontando la quote_originale dichiarata dal
            modello con il testo sorgente realmente scrapato (fuzzy match).

          GESTIONE LIMITI ENTERPRISE (v2):
            1. Token bucket per-chiave: il limite dichiarato e' TPM 65K. Ogni
               GeminiKeyPool.acquire() stima i token del payload e attende finche'
               non c'e' budget sufficiente nel bucket della chiave corrente, con
               un margine di sicurezza (75% del limite dichiarato) perche' la stima
               token e' approssimata (~4 char/token) e non il vero tokenizer.
            2. Multi-key round-robin con cooldown: se una chiave sforra comunque
               (risposta 1011 / "exceeded your current quota"), viene marcata in
               cooldown per COOLDOWN_SECONDS e il pool passa automaticamente alla
               chiave successiva disponibile. Funziona anche con una sola chiave:
               in quel caso il "pool" ha un solo elemento e la chiave va semplicemente
               in attesa che il proprio bucket si ricarichi.
            3. Scrittura incrementale JSONL: ogni nugget estratto viene appeso
               immediatamente su disco (una riga = un oggetto JSON completo), NON
               accumulato solo in RAM. Se il processo viene interrotto (timeout job,
               kill, crash), tutto cio' che era stato scritto fino a quel momento
               resta valido e recuperabile.
          """

          import json
          import re
          import os
          import time
          import asyncio
          import difflib

          from google import genai

          MODEL_NAME = "gemini-3.1-flash-live-preview"

          CONFIG_LIVE = {
              "response_modalities": ["AUDIO"],
              "output_audio_transcription": {}
          }

          # Timeout di sicurezza per singola chiamata Live (per-target, non per-chunk)
          NUGGET_TIMEOUT_SECONDS = 40.0

          # Limite dichiarato dal piano: TPM 65.000. Usiamo un margine di sicurezza
          # perche' la stima token e' approssimata (char/4), non il tokenizer reale.
          TPM_LIMIT_DICHIARATO = 65_000
          TPM_SAFETY_MARGIN = 0.75
          TPM_BUDGET = int(TPM_LIMIT_DICHIARATO * TPM_SAFETY_MARGIN)  # 48.750

          # Quanto tempo una chiave resta "in cooldown" dopo aver sforato il limite
          # (segnale ricevuto direttamente dall'errore 1011 di Gemini, non dedotto)
          COOLDOWN_SECONDS = 65.0

          # Segnali testuali che identificano un errore di quota/rate-limit nella
          # risposta d'errore della Live API (messaggio WebSocket 1011)
          QUOTA_ERROR_MARKERS = ("exceeded your current quota", "resource_exhausted", "rate limit")

          NUGGET_PROMPT_TEMPLATE = """
          Sei un estrattore di fatti (nugget) ad alta precisione per un sistema editoriale
          di ricerca. Il tuo compito e' leggere il TESTO SORGENTE fornito ed estrarre
          SOLO informazioni fattuali, dati concreti o affermazioni verificabili che
          potrebbero essere utili per un articolo editoriale sul TARGET TOPIC indicato.

          REGOLE:
          1. Estrai SOLO fatti realmente presenti nel testo. Non inventare, non
             dedurre, non completare informazioni mancanti.
          2. Per ogni nugget, riporta in "quote_originale" la frase o porzione di
             testo ESATTA (verbatim, non parafrasata) da cui hai estratto il fatto.
             Questo campo verra' verificato programmaticamente contro il testo
             sorgente: se non corrisponde, il nugget verra' scartato.
          3. Classifica ogni nugget in una "category" tra: prezzo, orario, evento,
             storia, geografia, recensione, dato_statistico, contatto, altro.
          4. Se il testo non contiene alcun fatto utile o rilevante, restituisci un
             array vuoto [].
          5. Non includere opinioni editoriali, pubblicita', o contenuto promozionale
             generico privo di dati concreti.

          Restituisci ESCLUSIVAMENTE un array JSON con oggetti nel formato:
          [
            {{"fact": "...", "category": "...", "quote_originale": "..."}}
          ]
          Usa il formato markdown ```json ... ```. Non aggiungere altro testo di commento.

          TARGET TOPIC:
          {topic}

          URL SORGENTE:
          {source_url}

          TESTO SORGENTE:
          {testo}
          """


          def _stima_token(testo):
              """Stima approssimata dei token (~4 caratteri/token per testo misto
              italiano/inglese). Deliberatamente prudente: meglio sovrastimare che
              sforare il limite reale."""
              if not testo:
                  return 0
              return max(1, len(testo) // 4)


          class TokenBucket:
              """Token bucket a ricarica continua per il rate limiting TPM di una
              singola API key. Non e' un pacing a intervallo fisso: permette raffiche
              quando il budget e' pieno e rallenta automaticamente quando si avvicina
              al limite, aspettando solo il tempo necessario a liberare budget."""

              def __init__(self, capacity_tokens, refill_period_seconds=60.0):
                  self.capacity = capacity_tokens
                  self.tokens = capacity_tokens
                  self.refill_period = refill_period_seconds
                  self.refill_rate = capacity_tokens / refill_period_seconds  # token/secondo
                  self.last_refill = time.monotonic()
                  self._lock = asyncio.Lock()

              def _refill(self):
                  now = time.monotonic()
                  elapsed = now - self.last_refill
                  if elapsed > 0:
                      self.tokens = min(self.capacity, self.tokens + elapsed * self.refill_rate)
                      self.last_refill = now

              async def acquire(self, needed_tokens, label=""):
                  """Attende finche' non c'e' budget sufficiente, poi lo consuma."""
                  needed_tokens = min(needed_tokens, self.capacity)  # non bloccare mai indefinitamente
                  async with self._lock:
                      while True:
                          self._refill()
                          if self.tokens >= needed_tokens:
                              self.tokens -= needed_tokens
                              return
                          deficit = needed_tokens - self.tokens
                          wait_seconds = deficit / self.refill_rate
                          wait_seconds = min(wait_seconds, 20.0)  # non aspettare mai troppo in un colpo solo
                          print(f"{label}[TPM-BUCKET] Budget insufficiente "
                                f"({self.tokens:.0f}/{needed_tokens} token), attendo {wait_seconds:.1f}s...",
                                flush=True)
                          await asyncio.sleep(wait_seconds)


          class GeminiKeyPool:
              """Gestisce una o piu' API key Gemini con round-robin, token bucket
              indipendente per chiave, e cooldown automatico sulle chiavi che
              sforano il limite. Funziona in modo trasparente anche con una sola
              chiave configurata."""

              def __init__(self, api_keys):
                  api_keys = [k for k in api_keys if k]
                  if not api_keys:
                      raise ValueError("Nessuna API key Gemini fornita")

                  self.entries = []
                  for key in api_keys:
                      self.entries.append({
                          "key": key,
                          "client": genai.Client(api_key=key),
                          "bucket": TokenBucket(TPM_BUDGET),
                          "cooldown_until": 0.0,
                      })
                  self._idx = 0
                  self._lock = asyncio.Lock()

              async def _pick_entry(self, label=""):
                  """Sceglie la prossima chiave disponibile (non in cooldown) in
                  round-robin. Se tutte sono in cooldown, aspetta quella che si
                  libera per prima."""
                  async with self._lock:
                      n = len(self.entries)
                      while True:
                          now = time.monotonic()
                          candidati = [(i, e) for i, e in enumerate(self.entries) if e["cooldown_until"] <= now]
                          if candidati:
                              for _ in range(n):
                                  self._idx = (self._idx + 1) % n
                                  entry = self.entries[self._idx]
                                  if entry["cooldown_until"] <= now:
                                      return entry
                          prossima_liberazione = min(e["cooldown_until"] for e in self.entries)
                          attesa = max(0.5, prossima_liberazione - now)
                          print(f"{label}[KEY-POOL] Tutte le chiavi in cooldown, attendo {attesa:.1f}s...",
                                flush=True)
                          await asyncio.sleep(min(attesa, 15.0))

              def _mark_cooldown(self, entry, label=""):
                  entry["cooldown_until"] = time.monotonic() + COOLDOWN_SECONDS
                  print(f"{label}[KEY-POOL] Chiave in cooldown per {COOLDOWN_SECONDS:.0f}s "
                        f"(quota TPM superata)", flush=True)

              async def esegui_con_pool(self, payload, timeout_seconds, label=""):
                  """Sceglie una chiave disponibile, attende budget TPM sufficiente
                  sul suo bucket, esegue la chiamata Live. Un SOLO tentativo su UNA
                  chiave: la logica di retry su piu' chiavi vive nel chiamante
                  (estrai_nuggets), non qui."""

                  entry = await self._pick_entry(label)
                  stima = _stima_token(payload)
                  await entry["bucket"].acquire(stima, label=label)

                  accumulated_text = ""
                  try:
                      async def run_session():
                          nonlocal accumulated_text
                          async with entry["client"].aio.live.connect(model=MODEL_NAME, config=CONFIG_LIVE) as session:
                              await session.send_realtime_input(text=payload)
                              async for response in session.receive():
                                  if response.server_content and response.server_content.output_transcription:
                                      if response.server_content.output_transcription.text:
                                          accumulated_text += response.server_content.output_transcription.text
                                  if response.server_content and response.server_content.turn_complete:
                                      break

                      await asyncio.wait_for(run_session(), timeout=timeout_seconds)
                      return accumulated_text.strip(), None

                  except (asyncio.TimeoutError, TimeoutError):
                      return None, "timeout"
                  except Exception as e:
                      err_str = str(e).lower()
                      if any(marker in err_str for marker in QUOTA_ERROR_MARKERS):
                          self._mark_cooldown(entry, label=label)
                          return None, "quota"
                      return None, f"errore: {e}"


          def _estrai_json_da_risposta(raw_text):
              if not raw_text:
                  return None
              match = re.search(r'```json\s*(.*?)\s*```', raw_text, re.DOTALL)
              json_str = match.group(1) if match else raw_text
              try:
                  parsed = json.loads(json_str)
                  if isinstance(parsed, list):
                      return parsed
              except Exception:
                  pass
              return None


          def _calcola_confidence(quote_originale, testo_sorgente):
              """Confidence derivata STRUTTURALMENTE, mai auto-riportata dal modello."""
              if not quote_originale or not testo_sorgente:
                  return "low"

              quote_norm = re.sub(r'\s+', ' ', quote_originale).strip().lower()
              testo_norm = re.sub(r'\s+', ' ', testo_sorgente).strip().lower()

              if not quote_norm:
                  return "low"

              if quote_norm in testo_norm:
                  return "high"

              finestra = len(quote_norm)
              if finestra < 8 or len(testo_norm) < finestra:
                  best_ratio = difflib.SequenceMatcher(None, quote_norm, testo_norm).ratio()
              else:
                  best_ratio = 0.0
                  step = max(1, finestra // 2)
                  for i in range(0, len(testo_norm) - finestra + 1, step):
                      frammento = testo_norm[i:i + finestra]
                      ratio = difflib.SequenceMatcher(None, quote_norm, frammento).ratio()
                      if ratio > best_ratio:
                          best_ratio = ratio
                      if best_ratio > 0.95:
                          break

              if best_ratio >= 0.85:
                  return "high"
              elif best_ratio >= 0.60:
                  return "medium"
              else:
                  return "low"


          def scrivi_nuggets_jsonl(nuggets, jsonl_path):
              """Scrittura incrementale: ogni nugget e' una riga JSON append-only.
              Se il file non esiste viene creato. Nessuna riscrittura del file intero:
              ogni chiamata aggiunge solo le righe nuove."""
              if not nuggets:
                  return
              with open(jsonl_path, "a", encoding="utf-8") as f:
                  for n in nuggets:
                      f.write(json.dumps(n, ensure_ascii=False) + "\n")


          async def estrai_nuggets(key_pool, source_url, testo_markdown, topic,
                                    node_id=None, timeout_seconds=NUGGET_TIMEOUT_SECONDS,
                                    max_key_attempts=None):
              """
              Estrae nugget strutturati da un singolo testo gia' scrapato, tramite
              Gemini Live, usando il pool di chiavi con token bucket TPM.

              Se una chiave sforra la quota, riprova automaticamente su un'altra
              chiave del pool (fino a max_key_attempts, default = numero di chiavi
              nel pool). Non solleva mai eccezioni verso il chiamante.

              Ritorna: lista di dict nugget con schema
                {fact, source_url, category, confidence, quote_originale}
              oppure None in caso di fallimento totale (tutte le chiavi esaurite/timeout).
              """
              if not testo_markdown or len(testo_markdown.strip()) < 50:
                  return []

              label = f"[NODO {node_id}] " if node_id is not None else ""
              payload = NUGGET_PROMPT_TEMPLATE.format(
                  topic=topic,
                  source_url=source_url,
                  testo=testo_markdown
              )

              if max_key_attempts is None:
                  max_key_attempts = len(key_pool.entries)

              ultimo_motivo = None
              for tentativo in range(max_key_attempts):
                  accumulated_text, errore = await key_pool.esegui_con_pool(payload, timeout_seconds, label=label)

                  if errore is None:
                      parsed = _estrai_json_da_risposta(accumulated_text)
                      if parsed is None:
                          print(f"{label}[NUGGET] Parsing JSON fallito su {source_url}. "
                                f"Output grezzo: {accumulated_text[:80]}...", flush=True)
                          return None

                      nuggets = []
                      for item in parsed:
                          if not isinstance(item, dict):
                              continue
                          fact = str(item.get("fact", "")).strip()
                          quote = str(item.get("quote_originale", "")).strip()
                          category = str(item.get("category", "altro")).strip() or "altro"
                          if not fact:
                              continue

                          confidence = _calcola_confidence(quote, testo_markdown)
                          nuggets.append({
                              "fact": fact,
                              "source_url": source_url,
                              "category": category,
                              "confidence": confidence,
                              "quote_originale": quote
                          })

                      print(f"{label}[NUGGET] {len(nuggets)} nugget estratti da {source_url}", flush=True)
                      return nuggets

                  elif errore == "quota":
                      ultimo_motivo = "quota"
                      print(f"{label}[NUGGET] Quota superata su {source_url}, "
                            f"provo con un'altra chiave ({tentativo + 1}/{max_key_attempts})...", flush=True)
                      continue

                  elif errore == "timeout":
                      print(f"{label}[NUGGET] TIMEOUT {timeout_seconds}s su {source_url}.", flush=True)
                      return None

                  else:
                      print(f"{label}[NUGGET] {errore} su {source_url}", flush=True)
                      return None

              print(f"{label}[NUGGET] Tutte le chiavi disponibili esaurite per {source_url} "
                    f"(ultimo motivo: {ultimo_motivo})", flush=True)
              return None
          EOF

      - name: Creazione Script Python - STEP 1 (Veloce curl-cffi, async + nugget extraction)
        run: |
          cat << 'EOF' > m2_step1.py
          import time
          import sys
          import random
          import os
          import json
          import asyncio
          from curl_cffi import requests as curl_requests
          from extraction_utils import html_to_clean_markdown
          from nugget_extractor import estrai_nuggets, GeminiKeyPool, scrivi_nuggets_jsonl

          DIAGNOSTIC_SITES = ["ipify", "browserleaks", "sannysoft", "creepjs", "pixelscan", "incolumitas", "deviceinfo"]

          TARGET_TOPIC = os.environ.get("TARGET_TOPIC", "")

          if len(sys.argv) < 2:
              sys.exit(1)
          node_id = int(sys.argv[1])
          ACTIVE_PORT = 40000 + node_id


          async def verify_proxy_active(port, max_retries=5, delay=5):
              test_session = curl_requests.AsyncSession(impersonate="chrome120")
              test_session.proxies = {"http": f"socks5h://127.0.0.1:{port}", "https": f"socks5h://127.0.0.1:{port}"}
              for _ in range(max_retries):
                  try:
                      resp = await test_session.get("https://api6.ipify.org?format=json", timeout=6)
                      if resp.status_code == 200:
                          await test_session.close()
                          return True
                  except Exception:
                      pass
                  await asyncio.sleep(delay)
              await test_session.close()
              return False


          def validate_html(html, url, status_code):
              if status_code >= 400:
                  return False, f"Blocco HTTP {status_code}"
              if not html or len(html.strip()) < 300:
                  return False, "HTML vuoto"
              if any(site in url for site in DIAGNOSTIC_SITES):
                  return True, ""

              low_html = html.lower()
              blocking_indicators = [
                  "challenge-platform", "access denied", "403 forbidden", "cloudflare",
                  "just a moment", "security verification", "please enable js", "enable javascript",
                  "are you a robot", "please wait for verification", "we've detected unusual activity",
                  "if you are not redirected", "unusual traffic from your computer network",
                  "checking your browser", "prove your humanity", "captcha verification",
                  "please enter in the characters you see below", "access to this page is forbidden",
                  "error 1020", "you have been blocked", "pardon our interruption",
                  "se non vieni reindirizzato", "se hai problemi ad accedere",
                  "robot challenge screen", "ci siamo quasi", "verifica di sicurezza"
              ]

              for ind in blocking_indicators:
                  if ind in low_html:
                      return False, f"WAF/Captcha rilevato: {ind}"
              return True, ""


          async def main():
              if not await verify_proxy_active(ACTIVE_PORT):
                  sys.exit(0)

              queue_file = f"/tmp/step1_queue_{node_id}.json"
              if not os.path.exists(queue_file):
                  sys.exit(0)

              with open(queue_file, "r") as f:
                  targets = json.load(f)

              api_key_env = os.environ.get("GEMINI_API_KEY", "")
              api_keys = [k.strip() for k in api_key_env.split(",") if k.strip()]
              gemini_key_pool = GeminiKeyPool(api_keys) if api_keys else None

              results = f"\n{'#'*60}\nREPORT NODO {node_id} - STEP 1 (curl-cffi)\n{'#'*60}\n"
              failed_targets = []
              all_nuggets = []
              nugget_retry_pool = []  # lista di tuple (url, testo_markdown)

              session = curl_requests.AsyncSession(impersonate="chrome120")
              session.proxies = {"http": f"socks5h://127.0.0.1:{ACTIVE_PORT}", "https": f"socks5h://127.0.0.1:{ACTIVE_PORT}"}
              session.headers.update({
                  "Accept-Language": "it-IT,it;q=0.9,en-US;q=0.8,en;q=0.7",
                  "Accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,*/*;q=0.8"
              })

              nugget_tasks = []

              async def lancia_nugget_task(url, testo_markdown):
                  """Crea un task fire-and-forget per l'estrazione nugget, cosi' il
                  loop di scraping puo' proseguire subito sul target successivo."""
                  if gemini_key_pool is None:
                      return

                  async def _run():
                      esito = await estrai_nuggets(
                          key_pool=gemini_key_pool,
                          source_url=url,
                          testo_markdown=testo_markdown,
                          topic=TARGET_TOPIC,
                          node_id=node_id
                      )
                      if esito is None:
                          nugget_retry_pool.append((url, testo_markdown))
                      else:
                          all_nuggets.extend(esito)
                          # Scrittura incrementale immediata su disco
                          scrivi_nuggets_jsonl(esito, f"nuggets_node_{node_id}_step1.jsonl")

                  task = asyncio.create_task(_run())
                  nugget_tasks.append(task)

              for url in targets:
                  print(f"[NODO {node_id}] [STEP 1] Richiesta: {url}", flush=True)
                  try:
                      resp = await session.get(url, timeout=12)
                      is_ok, err_msg = validate_html(resp.text, url, resp.status_code)

                      if is_ok:
                          if any(site in url for site in DIAGNOSTIC_SITES):
                              from bs4 import BeautifulSoup
                              soup = BeautifulSoup(resp.text, 'html.parser')
                              text = soup.get_text(separator=' ', strip=True)
                          else:
                              text = html_to_clean_markdown(resp.text, url)
                          results += f"\n{'='*50}\nTARGET: {url}\n{'='*50}\nTESTO:\n{text}\n"
                          print(f"[NODO {node_id}] -> Successo Rapido!", flush=True)

                          if not any(site in url for site in DIAGNOSTIC_SITES):
                              await lancia_nugget_task(url, text)
                      else:
                          print(f"[NODO {node_id}] -> Bloccato, passo allo Step 2: {err_msg}", flush=True)
                          failed_targets.append(url)
                  except Exception:
                      print(f"[NODO {node_id}] -> Errore rete. Passo allo Step 2.", flush=True)
                      failed_targets.append(url)

                  await asyncio.sleep(random.uniform(1.0, 2.0))

              await session.close()

              if nugget_tasks:
                  print(f"[NODO {node_id}] Attesa completamento {len(nugget_tasks)} chiamate nugget in volo...", flush=True)
                  await asyncio.gather(*nugget_tasks, return_exceptions=True)

              # Secondo (e ultimo) tentativo per i target andati in timeout/errore
              if nugget_retry_pool and gemini_key_pool is not None:
                  print(f"[NODO {node_id}] Retry nugget extraction su {len(nugget_retry_pool)} target falliti...", flush=True)
                  retry_tasks = []
                  for url, testo_markdown in nugget_retry_pool:
                      async def _retry(url=url, testo_markdown=testo_markdown):
                          esito = await estrai_nuggets(
                              key_pool=gemini_key_pool,
                              source_url=url,
                              testo_markdown=testo_markdown,
                              topic=TARGET_TOPIC,
                              node_id=node_id
                          )
                          if esito:
                              all_nuggets.extend(esito)
                              scrivi_nuggets_jsonl(esito, f"nuggets_node_{node_id}_step1.jsonl")
                          else:
                              print(f"[NODO {node_id}] [NUGGET] Retry fallito definitivamente su {url}", flush=True)
                      retry_tasks.append(asyncio.create_task(_retry()))
                  await asyncio.gather(*retry_tasks, return_exceptions=True)

              with open(f"output_node_{node_id}_step1.txt", "w", encoding="utf-8") as f:
                  f.write(results)
              with open(f"failed_node_{node_id}.json", "w", encoding="utf-8") as f:
                  json.dump(failed_targets, f)
              with open(f"nuggets_node_{node_id}_step1.json", "w", encoding="utf-8") as f:
                  json.dump(all_nuggets, f, indent=2, ensure_ascii=False)

              print(f"[NODO {node_id}] STEP 1 completato. Nugget totali estratti: {len(all_nuggets)}", flush=True)


          if __name__ == "__main__":
              asyncio.run(main())
          EOF

      - name: Esecuzione Scraping Parallelo - STEP 1
        run: |
          echo 'Avvio processi paralleli per STEP 1 (Velocità Pura)...'
          touch node1_step1.log node2_step1.log node3_step1.log node4_step1.log node5_step1.log node6_step1.log
          tail -q -f node1_step1.log node2_step1.log node3_step1.log node4_step1.log node5_step1.log node6_step1.log &
          TAIL_PID=$!
          
          PIDS=()
          for i in {1..6}; do
            python -u m2_step1.py $i > node${i}_step1.log 2>&1 &
            PIDS+=($!)
            sleep 1.5
          done

          wait "${PIDS[@]}"
          kill $TAIL_PID || true

      - name: Refresh IP Container per lo STEP 2
        run: |
          echo 'Riavvio dei container per pulire la sessione WARP per lo Step 2...'
          docker restart warproxy-1 warproxy-2 warproxy-3 warproxy-4 warproxy-5 warproxy-6
          sleep 6

      - name: Inizializzazione Code DISTRIBUITE - STEP 2
        run: |
          cat << 'EOF' > m2_step2_setup.py
          import json
          import os

          merged_failed = []
          for i in range(1, 7):
              filename = f"failed_node_{i}.json"
              if os.path.exists(filename):
                  with open(filename, "r", encoding="utf-8") as f:
                      try: merged_failed.extend(json.load(f))
                      except Exception: pass
          
          unique_failed = list(dict.fromkeys(merged_failed))
          
          for i in range(1, 7):
              chunk = unique_failed[i-1::6]
              with open(f"/tmp/step2_queue_{i}.json", "w") as f:
                  json.dump(chunk, f)
                  
          print(f"Code Step 2 distribuite con {len(unique_failed)} target da recuperare con CloakBrowser.")
          EOF
          python -u m2_step2_setup.py

      - name: Creazione Script Python - STEP 2 (Async + CloakBrowser Ottimizzato + nugget extraction)
        run: |
          cat << 'EOF' > m2_step2.py
          import asyncio
          import time
          import sys
          import random
          import os
          import json
          from urllib.parse import urlparse, parse_qs
          from cloakbrowser import launch_persistent_context_async
          from extraction_utils import html_to_clean_markdown
          from nugget_extractor import estrai_nuggets, GeminiKeyPool, scrivi_nuggets_jsonl

          if len(sys.argv) < 2: sys.exit(1)
          node_id = int(sys.argv[1])
          INITIAL_PORT = 40000 + node_id

          TARGET_TOPIC = os.environ.get("TARGET_TOPIC", "")

          def get_query_from_url(url, default="migliore carbonara roma"):
              try:
                  parsed = urlparse(url)
                  queries = parse_qs(parsed.query)
                  for param in ['q', 'query', 'p', 'find_desc']:
                      if param in queries: return queries[param][0]
              except: pass
              return default

          async def main():
              queue_file = f"/tmp/step2_queue_{node_id}.json"
              if not os.path.exists(queue_file): return
              
              with open(queue_file, "r") as f: targets = json.load(f)
              if not targets:
                  with open(f"output_node_{node_id}_step2.txt", "w", encoding="utf-8") as f: f.write("")
                  with open(f"nuggets_node_{node_id}_step2.json", "w", encoding="utf-8") as f: json.dump([], f)
                  return

              api_key_env = os.environ.get("GEMINI_API_KEY", "")
              api_keys = [k.strip() for k in api_key_env.split(",") if k.strip()]
              gemini_key_pool = GeminiKeyPool(api_keys) if api_keys else None

              all_nuggets = []
              nugget_retry_pool = []  # (url, testo) andati in timeout/errore: un solo retry a fine nodo
              nugget_tasks = []

              async def lancia_nugget_task(url, testo_markdown):
                  if gemini_key_pool is None:
                      return

                  async def _run():
                      esito = await estrai_nuggets(
                          key_pool=gemini_key_pool,
                          source_url=url,
                          testo_markdown=testo_markdown,
                          topic=TARGET_TOPIC,
                          node_id=node_id
                      )
                      if esito is None:
                          nugget_retry_pool.append((url, testo_markdown))
                      else:
                          all_nuggets.extend(esito)
                          # Scrittura incrementale immediata su disco
                          scrivi_nuggets_jsonl(esito, f"nuggets_node_{node_id}_step2.jsonl")

                  nugget_tasks.append(asyncio.create_task(_run()))

              home_dir = os.path.expanduser("~")
              profile_dir = os.path.join(home_dir, f".cloak_profiles/node_{node_id}_step2")
              windows_fonts_dir = os.path.join(home_dir, ".local", "share", "fonts", "windows")
              
              base_args = [
                  "--no-sandbox",
                  "--disable-dev-shm-usage",
                  f"--fingerprint={random.randint(100000, 999999)}",
                  f"--fingerprint-fonts-dir={windows_fonts_dir}"
              ]

              print(f"[NODO {node_id}] Avvio singola sessione Persistente (Warmup + Scraping)...", flush=True)
              try:
                  context = await launch_persistent_context_async(
                      profile_dir,
                      headless=False,
                      humanize=True,
                      human_config={"mistype_chance": 0.05, "typing_delay": 100, "idle_between_actions": True},
                      proxy=f"socks5://127.0.0.1:{INITIAL_PORT}",
                      geoip=True,
                      viewport={"width": 1920, "height": 1080},
                      args=base_args
                  )
                  page = await context.new_page()
                  
                  await page.goto("https://www.google.com", wait_until="domcontentloaded", timeout=25000)
                  await asyncio.sleep(2)
                  await page.goto("https://www.reddit.com", wait_until="domcontentloaded", timeout=25000)
                  await asyncio.sleep(2)

              except Exception as e:
                  print(f"[NODO {node_id}] Errore critico avvio Browser: {e}")
                  return

              results = f"\n{'#'*60}\nREPORT NODO {node_id} - STEP 2 (Fallback)\n{'#'*60}\n"

              for url in targets:
                  print(f"[NODO {node_id}] Recupero Avanzato: {url}", flush=True)
                  try:
                      query_to_search = get_query_from_url(url)
                      
                      if "bing.com" in url:
                          await page.goto("https://www.bing.com/", wait_until="domcontentloaded", timeout=35000)
                          await asyncio.sleep(random.uniform(2.5, 4.0))
                          try:
                              search_box = page.locator("textarea[name='q'], input[name='q']").first
                              await search_box.fill(query_to_search) 
                              await page.keyboard.press("Enter")
                          except: await page.goto(url, wait_until="domcontentloaded", timeout=35000)
                              
                      elif "startpage.com" in url:
                          await page.goto("https://www.startpage.com/", wait_until="domcontentloaded", timeout=35000)
                          await asyncio.sleep(random.uniform(2.0, 3.5))
                          try:
                              search_box = page.locator("input#q").first
                              await search_box.fill(query_to_search)
                              await page.keyboard.press("Enter")
                          except: await page.goto(url, wait_until="domcontentloaded", timeout=35000)
                          
                      elif "html.duckduckgo.com" in url:
                          await page.goto("https://html.duckduckgo.com/", wait_until="domcontentloaded", timeout=35000)
                          await asyncio.sleep(random.uniform(2.0, 3.5))
                          try:
                              search_box = page.locator("input[name=q]").first
                              await search_box.fill(query_to_search)
                              await page.keyboard.press("Enter")
                          except: await page.goto(url, wait_until="domcontentloaded", timeout=35000)
                          
                      else:
                          await page.goto(url, wait_until="domcontentloaded", timeout=40000)
                      
                      try: await page.wait_for_load_state("networkidle", timeout=12000)
                      except: pass
                          
                      await asyncio.sleep(6) 
                      
                      await page.keyboard.press("PageDown")
                      await asyncio.sleep(1)
                      await page.keyboard.press("PageDown")
                      await asyncio.sleep(2)
                      
                      raw_html = await page.content()
                      current_url = page.url

                      text_for_check = html_to_clean_markdown(raw_html, current_url)
                      text_lower_for_check = text_for_check.lower()
                      
                      blocking_indicators = [
                          "just a moment", "are you a robot", "prove your humanity",
                          "captcha verification", "please enter in the characters you see below",
                          "access is forbidden", "access to this page is forbidden", "challenge-platform",
                          "we've detected unusual activity"
                      ]
                      is_blocked = any(b in text_lower_for_check for b in blocking_indicators)
                      
                      if len(text_for_check) > 150 and not is_blocked:
                          results += f"\n{'='*50}\nTARGET: {url}\n{'='*50}\nTESTO ESTRATTO:\n{text_for_check}\n"
                          print(f"[NODO {node_id}] -> Recuperato con Successo!", flush=True)

                          await lancia_nugget_task(url, text_for_check)
                      else:
                          results += f"\n{'='*50}\nTARGET: {url}\n{'='*50}\nFALLITO (Blocco WAF/Captcha)\n"
                          print(f"[NODO {node_id}] -> Fallito.", flush=True)

                  except Exception as e:
                      print(f"[NODO {node_id}] -> Errore esecuzione: {e}", flush=True)
                      results += f"\n{'='*50}\nTARGET: {url}\n{'='*50}\nERRORE: {e}\n"
                  
                  await asyncio.sleep(random.uniform(4.0, 7.0))

              try: await context.close()
              except: pass

              if nugget_tasks:
                  print(f"[NODO {node_id}] Attesa completamento {len(nugget_tasks)} chiamate nugget in volo...", flush=True)
                  await asyncio.gather(*nugget_tasks, return_exceptions=True)

              if nugget_retry_pool and gemini_key_pool is not None:
                  print(f"[NODO {node_id}] Retry nugget extraction su {len(nugget_retry_pool)} target falliti...", flush=True)
                  retry_tasks = []
                  for url, testo_markdown in nugget_retry_pool:
                      async def _retry(url=url, testo_markdown=testo_markdown):
                          esito = await estrai_nuggets(
                              key_pool=gemini_key_pool,
                              source_url=url,
                              testo_markdown=testo_markdown,
                              topic=TARGET_TOPIC,
                              node_id=node_id
                          )
                          if esito:
                              all_nuggets.extend(esito)
                              scrivi_nuggets_jsonl(esito, f"nuggets_node_{node_id}_step2.jsonl")
                          else:
                              print(f"[NODO {node_id}] [NUGGET] Retry fallito definitivamente su {url}", flush=True)
                      retry_tasks.append(asyncio.create_task(_retry()))
                  await asyncio.gather(*retry_tasks, return_exceptions=True)

              with open(f"output_node_{node_id}_step2.txt", "w", encoding="utf-8") as f:
                  f.write(results)
              with open(f"nuggets_node_{node_id}_step2.json", "w", encoding="utf-8") as f:
                  json.dump(all_nuggets, f, indent=2, ensure_ascii=False)

              print(f"[NODO {node_id}] STEP 2 completato. Nugget totali estratti: {len(all_nuggets)}", flush=True)

          if __name__ == "__main__":
              asyncio.run(main())
          EOF

      - name: Esecuzione Scraping Parallelo - STEP 2 (Fallback CloakBrowser)
        run: |
          echo 'Avvio processi paralleli per STEP 2 sui target falliti...'
          touch node1_step2.log node2_step2.log node3_step2.log node4_step2.log node5_step2.log node6_step2.log
          tail -q -f node1_step2.log node2_step2.log node3_step2.log node4_step2.log node5_step2.log node6_step2.log &
          TAIL_PID=$!
          
          PIDS=()
          for i in {1..6}; do
            python -u m2_step2.py $i > node${i}_step2.log 2>&1 &
            PIDS+=($!)
            sleep 1.5
          done

          wait "${PIDS[@]}"
          kill $TAIL_PID || true
          
          for i in {1..6}; do
            docker stop warproxy-$i || true
            docker rm warproxy-$i || true
          done

          touch output_node_1_step1.txt output_node_1_step2.txt \
                output_node_2_step1.txt output_node_2_step2.txt \
                output_node_3_step1.txt output_node_3_step2.txt \
                output_node_4_step1.txt output_node_4_step2.txt \
                output_node_5_step1.txt output_node_5_step2.txt \
                output_node_6_step1.txt output_node_6_step2.txt

          cat output_node_1_step1.txt output_node_1_step2.txt \
              output_node_2_step1.txt output_node_2_step2.txt \
              output_node_3_step1.txt output_node_3_step2.txt \
              output_node_4_step1.txt output_node_4_step2.txt \
              output_node_5_step1.txt output_node_5_step2.txt \
              output_node_6_step1.txt output_node_6_step2.txt > output_macchina2_cdp.txt

      - name: Merge Nugget Finale (JSON strutturato deduplicato)
        run: |
          cat << 'EOF' > merge_nuggets.py
          import json
          import os

          TOTAL_NODES = 6

          merged_nuggets = []
          per_node_stats = {}

          for i in range(1, TOTAL_NODES + 1):
              for step in ["step1", "step2"]:
                  jsonl_filename = f"nuggets_node_{i}_{step}.jsonl"
                  json_filename = f"nuggets_node_{i}_{step}.json"
                  
                  nuggets = []
                  # Priorità assoluta al caricamento incrementale da JSONL (fault-tolerant)
                  if os.path.exists(jsonl_filename):
                      try:
                          with open(jsonl_filename, "r", encoding="utf-8") as f:
                              for line in f:
                                  line = line.strip()
                                  if line:
                                      try:
                                          nuggets.append(json.loads(line))
                                      except Exception:
                                          pass
                          per_node_stats[f"node_{i}_{step}"] = len(nuggets)
                          merged_nuggets.extend(nuggets)
                          continue  # Evita di caricare il file .json se il .jsonl esiste ed è valido
                      except Exception as e:
                          print(f"Errore lettura JSONL {jsonl_filename}: {e}", flush=True)

                  # Fallback sul dump JSON classico
                  if os.path.exists(json_filename):
                      try:
                          with open(json_filename, "r", encoding="utf-8") as f:
                              data = json.load(f)
                              if isinstance(data, list):
                                  merged_nuggets.extend(data)
                                  per_node_stats[f"node_{i}_{step}"] = len(data)
                      except Exception as e:
                          print(f"Errore lettura {json_filename}: {e}", flush=True)
                          per_node_stats[f"node_{i}_{step}"] = 0

          # Deduplica su (fact, source_url) normalizzati: evita nugget duplicati
          # quando lo stesso fatto appare identico da estrazioni diverse.
          seen = set()
          nuggets_deduplicati = []
          for n in merged_nuggets:
              fact_norm = (n.get("fact") or "").strip().lower()
              url_norm = (n.get("source_url") or "").strip().lower()
              key = (fact_norm, url_norm)
              if key in seen:
                  continue
              seen.add(key)
              nuggets_deduplicati.append(n)

          # Conteggio per livello di confidence, utile per audit rapido
          confidence_counts = {"high": 0, "medium": 0, "low": 0}
          for n in nuggets_deduplicati:
              c = n.get("confidence", "low")
              if c in confidence_counts:
                  confidence_counts[c] += 1

          output_finale = {
              "stats": {
                  "totale_nugget_grezzi": len(merged_nuggets),
                  "totale_nugget_deduplicati": len(nuggets_deduplicati),
                  "per_nodo": per_node_stats,
                  "per_confidence": confidence_counts
              },
              "nuggets": nuggets_deduplicati
          }

          with open("nuggets_finale.json", "w", encoding="utf-8") as f:
              json.dump(output_finale, f, indent=2, ensure_ascii=False)

          print(f"Merge completato: {len(nuggets_deduplicati)} nugget unici "
                f"(da {len(merged_nuggets)} grezzi) salvati in nuggets_finale.json", flush=True)
          print(f"Distribuzione confidence: {confidence_counts}", flush=True)
          EOF
          python -u merge_nuggets.py

      - name: Upload Artifact Macchina 2 (Testo Grezzo)
        uses: actions/upload-artifact@v4
        with:
          name: Risultati_Macchina2
          path: output_macchina2_cdp.txt

      - name: Upload Artifact Nugget (JSON Strutturato)
        uses: actions/upload-artifact@v4
        with:
          name: Nuggets_Macchina2
          path: nuggets_finale.json
